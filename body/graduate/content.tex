% 面向动物机器人自动导航的地面站-边缘端协同控制架构研究


\chapter{绪论}
\section{研究背景与意义}
动物机器人Animal Robot),又称赛博动物Cyborg Animal)或生物混合机器人
Biohybrid Robot),是一类将微电子神经接口植入活体动物神经系统,通过外部
控制指令调控动物行为,从而实现特定导航或探测任务的新型智能体。
与传统机械机器人相比,动物机器人天然具备生物感知能力强、运动适应性高、
能量利用效率优越等显著优势,在灾害搜救、反恐排爆、地形探测等复杂非结构化
场景中展现出广阔的应用前景,因此自本世纪初以来持续受到机器人学、神经工程学
与智能控制领域的广泛关注\cite{talwar2002rat}。
 
在动物机器人的典型实现方案中,大鼠被认为是最具代表性的载体之一。
其神经系统结构相对清晰,电刺激效果稳定且可重复,已成为该领域最为深入研究的
对象\cite{zheng2013rat}。
浙江大学郑筱祥团队自2007年起长期深耕大鼠机器人研究,先后提出了基于视觉线索
引导的自动导航方案\cite{wang2015visual}、基于有限状态机的自主搜索系统
\cite{zheng2023fsm},以及近年来提出的无人机引导大鼠协同导航
Rat-UAV Navigation)范式\cite{zheng2024ratuav},逐步将大鼠机器人从简单
遥控推进至自主导航阶段。
 
然而,上述研究在系统架构层面存在一个共性局限:现有大鼠机器人控制系统普遍
沿用"个人计算机PC)集中处理\,+\,蓝牙点对点通信"的单体架构模式。
在这一架构下,感知、决策与控制指令下发均由地面端单台主机承担,大鼠背包端
仅执行简单的刺激输出;蓝牙通信的往返延迟通常在100至500毫秒之间
\cite{zhang2019bci},有效通信距离普遍不超过5至10米\cite{feng2007remote}。
上述约束在简单实验室场景中尚可接受,但在Rat-UAV协同导航这一更具现实意义的
任务模式下,无人机UAV)飞行范围远超蓝牙覆盖半径,链路质量随飞行距离
和障碍物遮挡产生剧烈波动,原有单体架构的实时性与可靠性均难以满足任务需求,
成为制约动物机器人走向实际部署的核心瓶颈。
 
与此同时,分布式边缘计算与微服务架构在机器人领域的快速发展为上述问题的
解决提供了新的技术路径。以云机器人Cloud Robotics)\cite{kehoe2015cloud}
和边缘加速机器人导航Edge Accelerated Robot Navigation,EARN)
\cite{li2025earn}为代表的研究表明,将计算密集型任务卸载至高算力节点,
同时在本地边缘端保留实时控制逻辑,可以在资源受限场景下有效降低端到端
控制延迟并提升系统可扩展性。然而,这些方案主要面向工业机器人或无人机等
确定性执行器,尚未针对动物机器人背包端带宽极窄、生物行为不确定性高等
特殊约束进行适应性设计,因此无法直接迁移至大鼠机器人控制场景。
 
基于上述背景,本文以浙江大学Rat-UAV协同导航范式为应用背景,针对动物机器人
自动导航任务中控制系统架构与通信协议两个核心维度的空白,开展面向地面站-边缘端
协同控制的分布式架构研究。本文的研究成果不仅能够为动物机器人实验提供更高性能
的控制平台,也将为分布式系统理论在生物混合机器人领域的工程化落地提供参考。


\section{国内外研究现状}
本节从三个相互独立且共同构成分布式协同控制系统完整图景的维度,系统梳理
国内外相关研究进展:首先介绍动物机器人控制任务的背景与典型系统,继而综述
分布式机器人控制架构的主要范式与演进脉络,最后分析实时通信协议的研究现状,
为后续研究问题的提出奠定文献基础。
\subsection{动物机器人控制任务背景}
动物机器人研究的起点通常追溯至2002年Talwar等人在《自然》杂志发表的开创性
工作\cite{talwar2002rat}:通过向大鼠体感皮层和内侧前脑束植入微电极,
利用无线遥控实现了活体大鼠在复杂地形中的可控导航,首次证明了外部电信号
干预动物行为的可行性,开创了"Robo-Rat"研究方向。
此后,以蟑螂\cite{ariyanto2024cockroach}、熊蜂\cite{PlaceholderBumbleBee}、
信鸽等为载体的多种动物机器人相继被报道,展现了该研究方向的广泛适用性。
 
在大鼠机器人自主导航方面,浙江大学团队的系列工作代表了国内最高水平。
Wang等人于2015年提出视觉线索引导框架,将摄像头采集的图像经无线传输至
PC端进行目标检测,再将结果转化为刺激序列闭环控制大鼠转向\cite{wang2015visual};
Zheng等人于2023年提出基于有限状态机的大鼠自主导航系统,集成IMU与红外传感器,
在开放场地实现无辅助摄像头的自主搜索\cite{zheng2023fsm},并在论文中明确
指出"目前缺乏构建动物机器人自主导航系统的总体框架"。
这一论断直接揭示了系统架构层面的研究空白,是本文选题的重要动机之一。
最新的Rat-UAV Navigation工作进一步引入无人机作为移动观测平台,通过POMDP
框架处理大鼠行为不确定性\cite{zheng2024ratuav},但其软件架构仍为PC集中式
模式,边缘端协同尚未实现。
 
综上,现有动物机器人研究在控制算法层面已取得显著进展,但在控制系统架构方面,
自2002年至今普遍停留于PC集中处理\,+\,蓝牙点对点的单体模式,尚无面向分布式
协同的系统架构设计。这一空白构成本文的核心研究问题。

\subsection{分布式机器人控制系统研究}
分布式机器人控制系统的架构范式经历了从集中式、云机器人到边缘协同、再到
微服务化的多代演进。Medeiros等人的经典综述将移动机器人控制架构归纳为
反应式、审议式和混合式三大类\cite{medeiros1998survey},奠定了控制架构
研究的基础分类框架。随着网络技术的发展,Kehoe等人提出云机器人范式,将感知、
规划等高算力任务卸载至云端,机器人本体仅保留执行功能\cite{kehoe2015cloud},
显著提升了计算能力上限,但公有云的网络往返延迟通常超过50\,ms)限制了
其在实时控制场景中的应用。
 
针对云端延迟过高的问题,边缘计算范式被引入机器人控制领域。
FogROS2是这一方向的代表性工作,Ichnowski等人基于ROS\,2实现了计算节点向
云端或雾节点的透明迁移,在SLAM等任务中将延迟降低约50\%\cite{ichnowski2023fogros2}。
Li等人提出的EARN框架在机器人本地与边缘端之间动态切换运动规划器,在
Jetson Nano与Orin NX上验证了边缘加速的有效性\cite{li2025earn},
但其侧重于规划算法切换,缺乏微服务架构与通信协议层面的系统性设计。
针对UAV等资源受限飞行平台,Satpure等人将MPC控制器基于
Kubernetes\,+\,Docker容器化部署于边缘服务器,使控制频率从机载20\,Hz
提升至100\,Hz\cite{satpure2022mpc},验证了容器化在机器人边缘控制中的可行性。
 
在微服务架构方面,Xia等人首次将微服务引入云机器人系统,利用Docker容器将
机器人功能分解为协作微服务并通过Kubernetes管理\cite{xia2018microservice};
Schrick等人将微服务架构应用于安全关键移动机器人控制,证明微服务化可显著
提升系统并发性与容错能力\cite{schrick2024safety}。
然而,上述工作均在云端或资源充足的工业平台上实现,未涉及动物机器人背包端
资源极度受限通信带宽约200\,kbps、计算能力有限)的特殊场景,其方案无法
直接迁移。
 
综合来看,分布式机器人控制系统研究已在云机器人、边缘协同和微服务化三个
维度积累了丰富成果,但现有工作普遍面向工业机器人、无人机等确定性执行器,
其架构设计均未考虑生物机器人特有的不确定性和超低带宽约束。将上述分布式
架构理论适配至动物机器人场景,是本文需要重点解决的问题。

\subsection{实时通信协议在分布式控制中的研究}
通信协议是分布式机器人控制系统的神经网络,其延迟特性直接决定了系统的
控制实时性上限。现有机器人通信协议主要可分为三类:基于发布-订阅模型的
DDS/ROS\,2、基于请求-响应模型的gRPC/REST,以及面向IoT的MQTT等轻量级
协议\cite{profanter2019opcua}。
 
ROS\,2采用DDSData Distribution Service)作为中间件,其去中心化点对点
通信架构在理论上具有良好的实时性。然而,Maruyama等人的实测表明,ROS\,2
相比底层DDS可产生约50\%的延迟附加开销\cite{maruyama2016exploring};
后续研究进一步指出,标准ROS\,2无法在嵌入式设备上保证1\,ms通信周期
\cite{PlaceholderROS2Realtime2023}。
这一特性使ROS\,2难以满足动物机器人小于11\,ms端到端延迟的严苛要求。
 
% gRPC凭借HTTP/2多路复用与Protocol\,Buffers高效序列化,在微服务通信场景中
% 展现出显著的延迟优势。对比测试表明,gRPC响应时间比REST\,API快约77\%
% \cite{PlaceholderGrpcRest2024};Protocol\,Buffers序列化后消息体积约为
% JSON的$\frac{1}{2}$至$\frac{1}{3}$\cite{popic2016protobuf}。
% 然而,现有gRPC性能研究均在Web微服务或数据库读写场景中开展,缺乏在嵌入式
% 设备上、面向实时控制场景的系统性评估,也未涉及针对超窄带宽和远距离帧丢失
% 的协议级保护机制。
 
在动物机器人通信实践方面,现有系统普遍采用蓝牙作为地面站与背包端之间的
通信链路。Zhang等人的脑-脑接口大鼠控制实验实测显示,蓝牙控制指令延迟达
155至495\,ms\cite{zhang2019bci};BLE在工业IoT场景中的评估同样表明,
单帧传输延迟约为10\,ms,大数据包传输延迟可达400\,ms至1\,s,且无法保证
最大传输延迟的确定性上界\cite{PlaceholderBLE2017}。
上述数据定量揭示了蓝牙方案在实时性和可靠性方面的根本局限,为本文引入基于
gRPC扩展协议的替代方案提供了充分动机。
 
综上所述,gRPC协议在延迟性能和序列化效率上具有显著优势,但其标准实现
存在协议头开销偏重、缺乏控制帧优先级保护和坏帧恢复机制等不足,尚未针对
动物机器人远距离导航的超窄带宽场景进行适应性扩展。这一研究空白是本文
提出mice-gRPC协议的核心动机。

\section{本文主要研究内容}

基于上述研究现状的梳理,本文面向动物机器人自动导航任务,以浙江大学
Rat-UAV协同导航范式为应用背景,针对地面站-边缘端协同控制架构和远距离
低延迟通信协议两个核心问题展开研究。本文的主要研究内容如下:
 
\begin{enumerate}[leftmargin=2em, label=(\arabic*)]
 
  \item \textbf{面向动物机器人的三层异构协同控制架构设计。}
  在Rat-UAV协同范式基础上,设计并实现"地面站高算力决策)$-$UAV边缘端
  实时感知与中继)$-$大鼠背包端刺激执行)"三层异构分布式架构,
  以微服务形式部署Control Service与Stream Service,通过Docker多架构容器化
  实现x86地面站与ARM64 Jetson Orin边缘节点的跨平台统一管理,
  为两个核心创新点提供运行基础。
 
  \item \textbf{面向弱网环境的低时延任务调度算法WLTS)。}
  针对UAV飞行过程中链路质量波动导致任务卸载延迟不可预测的问题,提出
  基于链路质量感知的静态-动态混合任务调度算法。算法将实时控制任务固定
  保留于UAV边缘端执行,计算密集型任务视觉处理、路径规划)依据链路
  健康度三级状态机动态决策卸载至地面站或本地降级执行,并引入容错降级
  机制保证弱网条件下控制不中断。
 
  \item \textbf{面向远距离导航的mice-gRPC通信协议。}
  针对大鼠背包端蓝牙通信带宽极窄约200\,kbps)、远距离传输帧丢失率高、
  标准gRPC协议头开销与控制帧抢占问题,在gRPC协议基础上提出
  mice-gRPCMinimal IoT Control Extended gRPC)扩展协议。
  协议创新点包括:针对控制指令设计的轻量化Protobuf帧格式
  目标帧大小小于20字节)、CRC16坏帧校验与ARQ优先级重传机制,
  以及控制指令与视频流的单通路时分复用调度算法。
 
\end{enumerate}
 
本文的两项核心创新点概括如下:
 
\begin{itemize}[leftmargin=2em]
 
  \item \textbf{创新点一:}
  提出面向弱网环境的低时延任务调度算法WLTS),首次将链路自适应任务调度
  机制引入动物机器人分布式控制系统,实现弱网条件下端到端控制延迟小于11\,ms,
  相较于现有蓝牙集中式方案100至500\,ms)提升90\%以上。
 
  \item \textbf{创新点二:}
  提出面向远距离导航的mice-gRPC通信协议,在标准gRPC基础上扩展轻量化帧
  设计、坏帧保护与单通路复用机制,支持导航距离突破原有蓝牙方案的3至5米
  限制、扩展至15米以上,为动物机器人远距离自主导航提供可靠通信保障。
 
\end{itemize}
\section{本文结构}

aaaaaaaaaaaaaaaaaaaa


\chapter{第2章 相关工作与技术综述}
实现一个面向动物机器人的地面站-边缘端分布式协同控制系统,需要在架构设计、任务调度与通信协议三个核心维度上提供系统性的理论支撑。三者相互依存:合理的分布式架构是任务调度得以运行的物理基础,高效的任务调度算法决定了架构的实时性能上限,而通信协议则是连接各层节点、保障指令可靠传输的底层神经。脱离任一维度的孤立研究均无法有效解决动物机器人远距离自主导航中的核心问题。
基于上述认识,本章围绕三类核心技术问题展开文献综述与理论分析。2.2节系统梳理分布式控制架构的设计方法,重点介绍从集中式到微服务化的范式演进、任务卸载理论与嵌入式微服务部署实践;2.3节从协议选型角度分析实时通信协议的性能特征,重点阐述gRPC协议原理与面向资源受限环境的优化方向;2.4节以问题驱动的视角剖析现有动物机器人信息传输系统的共性架构与量化局限,论证引入分布式协同架构的必要性。2.5节对本章内容作综合性小结,并指出现有研究存在的空白。

\section{分布式控制架构的设计方法}
本节从机器人控制架构的历史演进出发,依次介绍集中式到微服务化的范式转变、边缘协同架构的任务卸载理论,以及面向嵌入式设备的微服务部署实践,为本文三层异构协同控制架构的设计提供理论依据。

\subsection{机器人控制架构的范式演进}
机器人控制架构的研究伴随着计算机技术和网络技术的发展经历了显著的范式演进,从早期的集中式单体架构,逐步向分布式、云化与微服务化方向演进。理解这一演进脉络,是把握本文架构设计取舍的前提。
集中式架构与分布式架构的分野。 Medeiros 等人在经典综述中将移动机器人控制架构归纳为反应式Reactive)、审议式Deliberative)和混合式Hybrid)三大范式 [Medeiros et al., 1998]。早期机器人系统多采用集中式审议架构:单台主机承担感知、决策与控制指令下发的全部职责,结构简单、调试便捷,但随着任务复杂度的提升,集中式架构的单点瓶颈日益凸显——计算资源上限受限于单机算力,通信链路的故障即导致系统全局失效,且难以支持多机协同场景下的并发控制需求。
云机器人范式的兴起。 随着云计算基础设施的普及,Kehoe 等人系统提出了云机器人Cloud Robotics)范式:将感知数据处理、路径规划、模型推理等计算密集型任务卸载至云端服务器,机器人本体仅保留执行层功能,从而突破单机算力上限 [Kehoe et al., 2015]。云机器人范式显著提升了系统可扩展性,并为多机器人协同提供了统一的数据共享平台。然而,云端数据中心与机器人本体之间的网络往返延迟通常超过50 ms,在实时控制场景中难以满足严苛的时序要求,这一固有局限催生了边缘计算在机器人领域的应用需求。
边缘协同与雾计算范式。 以FogROS2为代表的研究将计算节点从公有云下沉至网络边缘,显著缩短了控制环路延迟。Ichnowski 等人基于 ROS 2 实现了计算节点向云端或雾节点的透明迁移,在 SLAM 和抓取规划等任务中延迟降低约50\%,运动规划加速达28至45倍 [Ichnowski et al., 2023]。与此同时,Macenski 等人正式发布 ROS 2 系统设计论文,系统阐述了其基于 DDS 的去中心化点对点通信、QoS 策略与跨平台支持等架构特性 [Macenski et al., 2022]。上述工作共同表明,边缘协同范式在降低控制延迟方面具有显著优势,但依赖特定基础设施VPN、DDS中间件)的部署方式在资源受限的嵌入式场景中仍存在适用性问题。
微服务化架构的引入。 微服务架构Microservice Architecture)将单体应用分解为一组职责单一、可独立部署的服务单元,通过轻量级接口进行协作,已在 Web 工程领域得到广泛验证。Xia 等人首次将微服务架构引入云机器人系统,利用 Docker 容器将机器人功能模块化,并通过 Kubernetes 进行编排管理,在 Visual SLAM 任务中验证了微服务化带来的灵活性与可扩展性提升 [Xia et al., 2018]。微服务化架构的引入为本文将控制功能Control Service)与流媒体功能Stream Service)解耦部署于 UAV 边缘端提供了直接的设计范式参照。
综合来看,机器人控制架构经历了"集中式→分布式→云化→边缘协同→微服务化"的完整演进链条。本文所构建的三层异构协同架构正是在吸收边缘协同与微服务化两大范式优势的基础上,针对动物机器人特殊约束进行适应性设计的产物。

\subsection{异构协同架构的任务分配理论}
任务卸载Task Offloading)是边缘协同架构的核心决策机制,指将部分或全部计算任务从资源受限的本地节点迁移至算力更强的远端节点边缘服务器或云端)执行的过程。本节梳理任务卸载的理论框架,并重点分析动态卸载策略在动物机器人控制场景中的适用性局限。
任务卸载的分类框架。 根据决策时机与灵活程度,任务卸载策略通常分为三类:

静态卸载:在系统部署阶段预先确定任务执行位置,运行时不再调整。该策略延迟确定性高,但缺乏对网络状态变化的适应能力;
动态卸载:在运行时根据网络质量、计算负载等实时指标动态调整任务执行位置。该策略自适应性强,但引入调度决策本身的延迟开销,且存在任务中途切换导致状态不一致的风险;
混合卸载:对不同类型任务采用差异化策略——实时性强的任务采用静态分配,计算密集型任务采用动态卸载。该策略在延迟确定性与资源利用率之间寻求平衡。

代表性工作分析。 Li 等人提出的 EARNEdge Accelerated Robot Navigation)框架在机器人本地保守规划器与边缘端高性能规划器之间动态切换,通过模型预测切换Model-Predictive Switching,MPS)最大化边缘加速收益 [Li et al., 2025]。Satpure 等人针对 UAV 场景,将模型预测控制MPC)控制器基于 Kubernetes 和 Docker 容器化部署于边缘服务器,控制频率从机载20 Hz提升至100 Hz [Satpure et al., 2022]。Suganya 等人提出面向 UAV 的动态任务卸载框架DTOE-AOF),以地面站作为系统枢纽,通过 Wi-Fi 和5G与边缘节点通信,边缘节点在地面站和 UAV 之间充当中介,实现了三层数据流的协同 [Suganya et al., 2024]。
动态卸载对动物机器人刺激控制的潜在危害。 上述动态卸载方案均以机械执行器为控制对象——舵机、推进器等设备对指令的时序偏差具有一定的容忍裕度。然而,动物机器人的神经电刺激控制对时序确定性要求显著更高:大鼠内侧前脑束的奖励刺激有效窗口通常在数十至数百毫秒量级,若因动态卸载决策引入不可预测的延迟抖动,将导致刺激-行为关联弱化乃至训练效果丧失。此外,生物体行为本身具有不确定性,动态卸载在切换执行节点时若出现中间状态不一致,可能引发指令丢失或重复刺激,对实验的可重复性产生严重干扰。
因此,本文在任务调度策略上采用静态-动态混合方案:神经刺激控制指令的下发任务固定静态)保留于 UAV 边缘端本地执行,以保证刺激时序的确定性;视觉处理、路径规划等计算密集型任务则依据链路质量指标进行动态卸载决策。这一策略的理论必要性正源于上述对纯动态卸载方案的局限性分析,其具体算法设计将在第4章详细阐述。
\subsection{面向嵌入式设备的微服务部署}
将微服务架构部署于嵌入式边缘设备,面临与云端部署显著不同的约束:计算资源有限Jetson Orin 等嵌入式平台 RAM 通常为8至32 GB,远低于云服务器)、存储空间受限、且须保证实时响应而非仅追求吞吐量。本节梳理该方向的代表性工作,并指出其对本文场景的适用性边界。
安全关键场景的微服务设计。 Schrick 等人将微服务架构应用于安全关键移动机器人控制,提出基于微服务和层次有限状态机HFSM)的控制框架,通过微服务化减少集中式逻辑,显著提升系统并发性、可中断性和容错能力 [Schrick et al., 2024]。其核心贡献在于将"单点失效"风险分散至各微服务单元,单个服务的故障不影响其他服务的正常运行,这一容错思想对本文的服务生命周期设计具有重要参考价值。
嵌入式系统的微服务框架。 针对嵌入式系统资源受限特点,IEEE COMPSAC 2021的研究提出了适用于嵌入式平台的轻量级微服务框架,设计了嵌入式服务模型、轻量通信协议和服务注册发现流程 [Placeholder\_COMPSAC2021],为在 Jetson Orin 等设备上部署微服务提供了可行性验证。Satpure 等人在 UAV 场景中验证了基于 Kubernetes 和 Docker 的边缘部署方案 [Satpure et al., 2022],证明容器化编排技术可在计算资源受限的边缘节点上稳定运行。
现有方案的适用性边界。 综合上述研究可以发现,现有嵌入式微服务方案均未针对超低带宽下行链路如大鼠背包端约200 kbps 的蓝牙通道)进行服务接口设计,其服务间通信均假设充足的局域网带宽环境。将上述方案直接应用于本文场景时,Control Service 与背包端之间的通信设计将成为性能瓶颈,标准微服务通信协议的协议头开销将严重挤占有限的控制信道带宽。这一未解决的问题,正是本文提出 mice-gRPC 轻量化通信协议的直接动机,相关协议设计将在第5章详细阐述。

\section{实时通信协议的选型依据}
本节从协议选型的工程视角出发,系统对比机器人控制领域主流通信协议的性能特征,重点分析 gRPC 协议的技术原理,并指出现有协议在资源受限环境下的优化缺口,为本文通信协议设计提供理论依据
\subsection{机器人控制通信协议演进}
机器人控制通信协议经历了从简单无线链路到标准化中间件、再到轻量化应用层协议的多代演进,不同协议在延迟、带宽、可靠性与部署复杂度等维度上呈现出显著差异。本节从以上四个维度对主流协议进行横向对比见表2.1),为本文的协议选型提供定量依据。
蓝牙/BLE 协议在动物机器人领域被广泛应用于背包端与地面站之间的无线通信,其优势在于功耗低、配对简单、硬件成本低廉。然而,Zhang 等人的脑-脑接口大鼠控制实验实测显示,蓝牙控制指令延迟达155至495 ms [Zhang et al., 2019],且单通路带宽约200 kbps,无法支持控制指令与视频流的并行传输;蓝牙有效通信距离通常不超过10 m,在 Rat-UAV 场景中 UAV 飞行半径大幅超出覆盖范围 [PlaceholderBLE2017]。
DDS/ROS 2 是机器人领域应用最广泛的中间件通信标准。ROS 2 采用 DDSData Distribution Service)实现去中心化点对点通信,支持丰富的 QoS 策略配置。然而,Maruyama 等人的实测表明,ROS 2 相比底层 DDS 可产生约50\%的延迟附加开销 [Maruyama et al., 2016],且在树莓派等嵌入式设备上无法保证1 ms 通信周期 [PlaceholderROS2Realtime2023],对本文小于11 ms 端到端延迟目标而言裕度不足。
MQTT 是面向物联网场景的轻量级发布-订阅协议,协议头开销极小最小2字节),适合带宽极受限的传感器网络。但 MQTT 默认基于 TCP,缺乏原生的请求-响应语义和流式传输能力,难以直接用于需要双向同步控制的机器人场景 [Profanter et al., 2019]。
gRPC 基于 HTTP/2 和 Protocol Buffers,在微服务通信场景中展现出显著的延迟优势。对比测试表明,gRPC 响应时间比 REST API 快约77\% [PlaceholderGrpcRest2024],其多路复用特性支持单连接上的并发 RPC 调用,Protocol Buffers 序列化消息体积约为 JSON 的1/2至1/3 [Popic et al., 2016],在延迟、带宽利用率和接口规范化方面均具有明显优势。

表2.1 机器人控制主流通信协议横向对比
协议名称典型延迟带宽占用可靠性机制适用场景主要局限蓝牙/BLE155–495 ms [Zhang et al., 2019]约200 kbps链路层重传短距低功耗IoT延迟高、距离短、单通路DDS/ROS 2基础延迟+50\%开销 [Maruyama et al., 2016]较高协议头重)QoS多级保障工业机器人、多机协同嵌入式设备开销大MQTT<10 ms局域网)极低2字节头)QoS 0/1/2级物联网传感器网络缺乏双向同步语义gRPC<5 ms局域网)[PlaceholderGrpcRest2024]低Protobuf序列化)HTTP/2流控微服务RPC调用远距离无帧保护机制REST/HTTP比gRPC慢约77\% [PlaceholderGrpcRest2024]高JSON/XML)应用层重试Web API延迟高、序列化开销大

综合上述对比可以看出,gRPC 在延迟性能和序列化效率方面具有最显著的综合优势,是本文地面站-边缘端间通信的基础选型。然而,gRPC 标准实现在远距离低带宽场景下的可靠性保障机制仍存在明显不足,这将在2.3.3节进一步分析。

\subsection{gRPC协议性能分析与适用性}
gRPCGoogle Remote Procedure Call)是 Google 开源的高性能 RPC 框架,基于 HTTP/2 传输层和 Protocol BuffersProtobuf)序列化协议构建,已成为微服务通信领域的事实标准之一。本节从协议原理和性能特征两个层面进行深入分析。
HTTP/2 多路复用原理。 gRPC 基于 HTTP/2 协议,其核心优势在于多路复用Multiplexing)机制:在单条 TCP 连接上,HTTP/2 通过流Stream)机制支持多个并发请求的交错传输,彻底消除了 HTTP/1.1 中因请求串行化导致的队头阻塞Head-of-Line Blocking)问题。对于本文场景,这意味着控制指令 RPC 调用与状态上报 RPC 调用可以在同一 TCP 连接上并发执行,无需建立多条独立连接,显著降低了连接建立的开销。
Protocol Buffers 序列化机制。 gRPC 默认使用 Protocol BuffersProtobuf)作为接口定义语言IDL)和序列化格式。Protobuf 采用变长编码Varint)对整型字段进行压缩,序列化后的消息体积约为等效 JSON 的1/3至1/2,序列化速度显著快于 JSON 和 XML [Popic et al., 2016]。在带宽约为200 kbps 的背包端蓝牙链路上,消息体积的压缩对于提升有效控制帧传输速率具有直接意义。
Unary RPC 与 Server Streaming RPC 的选择逻辑。 gRPC 提供四种通信模式:Unary RPC单次请求-响应)、Server Streaming RPC服务端流)、Client Streaming RPC客户端流)和 Bidirectional Streaming RPC双向流)。在本文架构中,控制指令下发对实时性要求高、消息体积小,适合采用 Unary RPC 模式——单次 RPC 调用即可完成指令发送与执行确认的闭环,延迟开销最小;视频流数据具有持续性和高带宽特征,适合采用 Server Streaming RPC 模式——服务端持续推送帧数据,客户端无需反复发起请求,减少了连接建立开销。
嵌入式设备上的性能评估。 在 Jetson Orin 等 ARM64 嵌入式平台上,gRPC Python 实现的单次 Unary RPC 往返延迟在局域网环境下通常低于5 ms [PlaceholderGrpcRest2024],满足本文小于11 ms 端到端控制延迟的设计目标。相比之下,ROS 2 在同类嵌入式设备上的延迟附加开销约为底层 DDS 的50\% [Maruyama et al., 2016],难以满足相同的实时性要求。这一对比从协议性能角度支撑了本文选择 gRPC 而非 ROS 2 作为边缘端服务通信框架的技术决策。
\subsection{资源受限环境下的协议优化方向}
尽管 gRPC 在通用微服务场景中具有显著优势,但其标准实现在面向大鼠背包端超低带宽、远距离无线链路等资源受限环境时,仍存在若干尚未解决的工程问题。本节系统梳理相关优化方向,并指出现有研究的局限。
帧压缩与协议头精简。 HTTP/2 协议帧头部固定开销为9字节,Protobuf 消息在经过 gRPC 封装后,控制帧总大小通常在60至80字节范围内。在200 kbps 的蓝牙链路上,若以20 Hz 频率下发控制指令,仅控制帧即占用约12.8 kbps80字节x20 Hzx8 bit),考虑视频流、状态心跳等并发传输需求,带宽竞争将显著影响控制帧的及时送达。因此,针对控制帧的极致轻量化设计——压缩 Protobuf 字段、减少不必要字段填充——是资源受限场景下的重要优化方向。
优先级队列与控制帧保护。 标准 gRPC 基于 HTTP/2 的流量控制机制对所有 RPC 调用一视同仁,不区分控制帧与数据帧的传输优先级。当视频流Stream Service)与控制指令Control Service)共享同一网络链路时,大体量的视频帧极易占满带宽窗口,导致控制帧排队等待,引入不可预测的附加延迟。引入双优先级队列机制——控制帧进入高优先级队列、视频帧进入低优先级队列——是解决该问题的关键方向,但目前尚无面向动物机器人通信场景的实现方案。
ARQ 重传机制与坏帧保护。 标准 gRPC 依赖 TCP 的可靠传输保障,TCP 的重传机制虽然保证了最终可靠性,但在远距离无线场景中信号衰减导致帧错误率上升),TCP 的拥塞控制算法会将重传触发的超时误判为网络拥塞,进而降低发送窗口,造成吞吐量的大幅下降。面向无线链路的 ARQAutomatic Repeat reQuest)机制在应用层提供帧级错误检测与重传,可在不触发 TCP 拥塞控制的前提下快速恢复丢失帧,对于保障远距离控制指令的可靠性至关重要。
单通路多路复用。 大鼠背包端受体积与功耗限制,通常仅能搭载单一蓝牙通信模块,形成单通路约束。如何在单条蓝牙链路上同时传输控制指令、视频流和状态心跳三类数据,并保证各类数据的实时性需求,是现有 gRPC 框架未涉及的工程问题。时分多路复用Time-Division Multiplexing,TDM)结合帧调度算法是解决该问题的主要技术路径。
综合上述分析,现有 gRPC 标准协议在远距离低带宽场景中存在协议头开销偏重、无控制帧优先保护机制、缺乏应用层坏帧保护以及不支持单通路多优先级复用四个核心局限。这些局限在通用 Web 微服务场景中影响有限,但在大鼠背包端200 kbps 蓝牙链路和15 m以上导航距离的约束下将被显著放大,成为制约系统实时性与可靠性的关键瓶颈。这一理论缺口正是本文在第5章提出 mice-gRPC 协议的核心动机。


\section{动物机器人信息传输系统现状与问题}
本节以问题驱动的视角系统审视现有动物机器人信息传输系统的架构模式与量化性能,通过与 Rat-UAV 协同导航任务需求的对比,揭示现有系统在延迟、距离、带宽三个维度上的结构性局限,并从工程可行性角度论证引入分布式协同架构的必要性。
\subsection{现有动物机器人通信系统的共性架构}
尽管动物机器人控制算法经历了从简单遥控到闭环自主导航的显著进步,但其底层信息传输系统的架构模式自2002年以来几乎未发生根本性变化,普遍沿用"地面PC集中处理 + 蓝牙点对点传输"的单体通信模式。
典型系统的架构分析。 Feng 等人于2007年设计了基于蓝牙无线微刺激器的大鼠遥控系统,采用 C8051 微处理器驱动的蓝牙模块实现地面端与背包端的点对点通信,地面端 PC 承担全部信号处理与指令生成职责 [Feng et al., 2007]。Wang 等人于2015年提出的视觉线索引导框架将摄像头采集图像经无线传输至 PC 端进行目标检测,再将决策结果转化为刺激序列下发至背包端 [Wang et al., 2015]——其数据流本质仍为"背包传感→无线→PC处理→无线→背包执行"的单环路集中式模式。
Zhang 等人于2019年实现的脑-脑接口系统同样采用蓝牙作为唯一通信链路,实测控制指令延迟达155至495 ms [Zhang et al., 2019],尽管其应用场景人脑意念控制大鼠)与本文不同,但所采用的通信架构与性能瓶颈完全一致。Zheng 等人于2023年提出的基于有限状态机的大鼠自主导航系统在算法层面实现了重要突破,但其控制架构仍为单板嵌入式系统加集中式处理 [Zheng et al., 2023]。即便是最新的 Rat-UAV Navigation 工作,其软件控制架构依然以地面端 PC 为中心,UAV 仅充当视觉传感器的搭载平台,尚未承担边缘计算节点的功能 [Zheng et al., 2024]。
上述分析清晰表明,在23年的发展历程中,动物机器人控制系统在算法创新层面取得了丰硕成果,但在系统架构层面从未突破集中式单体模式的范式约束。

\subsection{现有系统的通信局限与研究空白}
以 Rat-UAV 协同导航任务为参照基准,本节从延迟、距离、带宽三个维度对现有蓝牙集中式方案进行量化对比分析,揭示其在当前任务场景下的结构性局限见表2.2)。
延迟维度。 现有系统的蓝牙通信延迟为155至495 ms [Zhang et al., 2019]。神经电刺激控制的有效时序要求通常在数十毫秒量级——奖励刺激需在大鼠完成目标行为后及时施加以建立正强化关联;若刺激延迟超过行为发生后约500 ms,条件化效果将显著下降 [PlaceholderNeurostim]。当前蓝牙方案的延迟上限495 ms)已接近神经刺激有效窗口的极限,且存在较大的延迟抖动,在 Rat-UAV 场景中 UAV 飞行引入的额外信号衰减将进一步恶化延迟表现。
距离维度。 蓝牙的有效通信距离在障碍物干扰环境中通常为3至10 m [PlaceholderBLE2017],而 Rat-UAV 协同导航场景中 UAV 的典型飞行半径在数十至百余米量级。两者之间存在一个数量级以上的距离差距,使现有蓝牙方案在 Rat-UAV 场景中从根本上不可用。
带宽维度。 蓝牙4.x 的物理层净荷吞吐量约为200 kbps,Rat-UAV 场景中需同时传输的数据包括:控制指令流约10–20 kbps)、摄像头视频流即使压缩后也需要数百 kbps)以及状态心跳约5 kbps)。三类数据的带宽需求之和远超蓝牙单通路的承载能力,导致现有系统不得不在控制指令与视频流之间作出取舍,无法实现两者的并行传输。

表2.2 现有蓝牙方案与 Rat-UAV 任务需求对比
指标现有蓝牙方案Rat-UAV 任务需求差距控制延迟155–495 ms [Zhang et al., 2019]<50 ms神经刺激时序要求)差距3–10倍通信距离3–10 m [PlaceholderBLE2017]>50 mUAV飞行半径)差距5–15倍可用带宽~200 kbps单通路)>500 kbps控制+视频+状态)差距>2.5倍多路复用不支持控制/视频/状态并行传输结构性缺失

\subsection{引入分布式协同架构的必要性论证}
面对上述量化局限,一个自然的问题是:能否直接借鉴工业机器人或云机器人领域已有的分布式架构方案加以解决？本节对两类候选方案的适用性进行逐一论证,得出必须针对动物机器人特殊约束重新设计的结论。
工业机器人分布式方案不可直接照搬。 工业机器人分布式控制方案如 FogROS2、CIRPO 等)均以确定性机械执行器为控制对象:电机、液压机构等设备对指令延迟的容忍度在数十至数百毫秒量级,且其执行结果具有精确的物理确定性。动物机器人则面临三项根本性差异:其一,生物体不确定性——大鼠个体在神经接口效果、疲劳程度、情绪状态等方面存在显著差异,控制系统须具备实时闭环反馈能力,不可采用开环批量指令模式;其二,背包端资源极度受限——大鼠背包受体积和重量限制通常不超过10 g [PlaceholderBackpackWeight]),仅能搭载超低功耗嵌入式单元,无法运行完整的 DDS 中间件或 Kubernetes 代理;其三,神经刺激对延迟确定性要求更高——工业执行器对50 ms以内的延迟变动通常不敏感,但神经刺激若在有效时序窗口外施加,将直接导致强化学习训练失效,系统须保证刺激指令的确定性时延上界。上述三点差异共同决定了工业机器人分布式方案无法直接迁移至动物机器人场景。
云机器人方案FogROS2)不可直接采用。 FogROS2 等云机器人平台通过公有云 VPN 实现计算节点的透明迁移,其网络往返延迟通常超过50 ms [Ichnowski et al., 2023],远不满足本文小于11 ms 控制延迟的设计目标。此外,FogROS2 依赖公有云基础设施,在 UAV 飞行于信号覆盖稀疏区域时将面临完全断连风险,且缺乏离线降级机制——一旦云端连接中断,控制系统将无法切换至本地模式保证基本导航功能。对于须在野外复杂环境中执行长时任务的 Rat-UAV 系统而言,这一脆弱性不可接受。
结论:必须针对动物机器人特殊约束设计专用分布式协同架构。 综合上述分析,动物机器人自动导航场景面临的约束组合——背包端超低带宽约200 kbps)、生物体不确定性要求的实时闭环、UAV 飞行的弱网/远距离通信、神经刺激严苛的时序确定性需求——共同构成了一个现有工业和云机器人分布式方案均无法覆盖的特殊约束空间。本文因此提出针对上述约束量身定制的三层异构协同控制架构,并在此基础上设计面向弱网的低时延任务调度算法WLTS,第4章)和面向远距离导航的 mice-gRPC 通信协议第5章),以系统性地突破上述瓶颈。

\section{本章小结}
本章围绕面向动物机器人地面站-边缘端分布式协同控制系统所需的三类核心技术基础,系统梳理了分布式控制架构设计方法、实时通信协议选型依据以及现有动物机器人信息传输系统的局限与研究空白。表2.3对三个方向的核心结论作综合性汇总。

表2.3 本章文献综述核心结论汇总
研究方向现有代表性工作核心局限本文对应解决方案分布式架构FogROS2 [Ichnowski et al., 2023]、微服务云机器人 [Xia et al., 2018]面向工业/云场景,未适配背包端超低带宽与离线降级需求三层异构协同控制架构第3章)任务调度EARN [Li et al., 2025]、DTOE-AOF [Suganya et al., 2024]纯动态卸载引入刺激时序不确定性,不适用于神经刺激控制弱网低时延静态-动态混合调度算法 WLTS第4章)通信协议蓝牙 [Zhang et al., 2019]、标准 gRPC [PlaceholderGrpcRest2024]蓝牙延迟高/距离短;gRPC 无控制帧优先保护与坏帧恢复机制mice-gRPC 轻量化扩展通信协议第5章)

上述三类问题相互关联、层层递进:架构设计确定了任务调度的边界条件,任务调度的正确执行依赖通信协议提供可靠的低延迟数据链路,而通信协议的设计又须在架构约束背包端资源限制、单通路通信)下进行优化。第3章将在本章理论基础之上,详细阐述面向动物机器人的三层异构协同控制架构的系统设计。

\chapter{第3章 面向弱网环境的低时延任务调度算法}

\section{引言}

第5章确立的三层异构协同控制架构将地面站的高算力决策与UAV边缘端的实时控制解耦部署,两者通过4G蜂窝网络连接。这一架构设计从根本上解决了蓝牙方案的距离和带宽瓶颈,但同时引入了一个在蓝牙方案中不存在的新问题:4G链路的质量不是稳定的。
UAV在飞行过程中,随着与基站距离的变化、障碍物遮挡的出现,4G信号强度会在数秒内发生剧烈波动。对于机械执行器而言,控制指令延迟从5 ms上升到80 ms通常不会造成灾难性后果——执行器有足够的惯性容纳这种抖动。但对于神经电刺激控制而言,刺激时序与大鼠行为之间存在严格的条件化关联窗口,一旦指令延迟超出这一窗口,强化学习的效果将显著下降。这意味着,现有面向机械机器人的动态任务卸载方案——无论是EARN \cite{li2025earn}还是其他基于模型预测的卸载框架——均无法直接迁移到动物机器人控制场景。它们的设计假设执行器对延迟不敏感,而本文的场景恰恰相反。
本章提出弱网感知低时延任务调度算法(Weak-network-aware Low-latency Task Scheduling,WLTS),核心思路是将任务分为"不可调度"和"可调度"两类:神经刺激控制指令的下发固定在边缘端本地执行,不参与任何动态卸载决策;视觉处理、路径规划等计算密集型任务则依据实时链路质量动态决策执行位置。这种静态-动态混合策略在保证刺激时序确定性的前提下,最大化利用地面站的算力资源。
本章结构安排如下:4.2节对端到端控制延迟进行形式化建模,建立链路质量的量化评估框架;4.3节详细阐述WLTS算法的三个功能模块;4.4节分析算法的理论性质;4.5节通过实验验证算法性能。

\section{问题建模与延迟分析}

\subsection{端到端控制延迟的构成分析}

在三层协同控制架构中,一条控制指令从地面站导航算法产生,到大鼠背包端完成神经电刺激,需要经过多个处理环节。将这一端到端延迟记为 TtotalT_{\text{total}}
Ttotal​,可分解为:

Ttotal=Tnet+Tedge+TserialT_{\text{total}} = T_{\text{net}} + T_{\text{edge}} + T_{\text{serial}}Ttotal​=Tnet​+Tedge​+Tserial​
其中各分量的定义和实测情况如表4.1所示。
表4.1 端到端控制延迟分量分析
延迟分量符号典型值是否随网络波动备注网络传输延迟TnetT_{\text{net}}
Tnet​WiFi近距P50=4.56 ms是(主要波动源)包含gRPC序列化+4G传播边缘端处理延迟TedgeT_{\text{edge}}
Tedge​约2 ms否servicer解析+串口帧组装串口通信延迟TserialT_{\text{serial}}
Tserial​约3 ms否蓝牙短距,距离<1 m端到端总计TtotalT_{\text{total}}
Ttotal​约9.56 ms(WiFi近距)—设计目标 <11 ms(P99)
这一分解揭示了问题的核心结构:TedgeT_{\text{edge}}
Tedge和 TserialT_{\text{serial}}
Tserial是系统固有开销,在当前硬件配置下基本稳定,合计约5 ms;留给网络传输的预算仅有 11-5=611 - 5 = 6
11-5=6 ms。在WiFi近距基线条件下,实测 TnetT_{\text{net}}
TnetP50=4.56 ms,系统具有约1.44 ms的裕量。然而一旦进入4G弱网场景,TnetT_{\text{net}}
Tnet可跃升至数十毫秒,系统需要通过调度策略将其影响控制在可接受范围内。


[PLACEHOLDER-4.2.1: 待4G模块(EC200A-CNHA)部署后,补充4G链路条件下 TnetT_{\text{net}}
Tnet的实测分布,包括不同距离(5/20/50 m)和不同遮挡条件下的P50/P95/P99值。这组数据将直接支撑4.2.2节的状态机阈值标定。]


\subsection{弱网场景下的链路质量建模}

仅靠单一指标(如RTT或丢包率)难以全面刻画4G链路的实时状态——RTT低但丢包率高,或RTT高但丢包率低,对系统的影响是不同的。本文定义综合链路质量指数 Q(t)∈[0,1]Q(t) \in [0, 1]
Q(t)∈[0,1],将两个维度融合为一个标量:

Q(t)=α⋅(1-PLR(t))+(1-α)⋅(1-RTT(t)RTTmax⁡)Q(t) = \alpha \cdot \bigl(1 - \text{PLR}(t)\bigr) + (1-\alpha) \cdot \left(1 - \frac{\text{RTT}(t)}{\text{RTT}_{\max}}\right)Q(t)=α⋅(1-PLR(t))+(1-α)⋅(1-RTTmax​RTT(t)​)
其中 PLR(t)\text{PLR}(t)
PLR(t) 为当前丢包率,RTT(t)\text{RTT}(t)
RTT(t) 为当前往返时延,RTTmax⁡\text{RTT}_{\max}
RTTmax为链路不可用阈值,α∈(0,1)\alpha \in (0,1)
α∈(0,1) 为权重系数。两项均被归一化至 [0,1][0,1]
[0,1],Q(t)Q(t)
Q(t) 越接近1表示链路质量越好。

为避免瞬时测量噪声引起状态频繁抖动,PLR(t)\text{PLR}(t)
PLR(t) 和 RTT(t)\text{RTT}(t)
RTT(t) 均采用指数加权移动平均(EWMA)计算:

RTT‾(t)=β⋅RTT(t)+(1-β)⋅RTT‾(t-1)\overline{\text{RTT}}(t) = \beta \cdot \text{RTT}(t) + (1-\beta) \cdot \overline{\text{RTT}}(t-1)RTT(t)=β⋅RTT(t)+(1-β)⋅RTT(t-1)
平滑系数 β\beta
β 控制对历史数据的遗忘速度,较小的 β\beta
β 使估计更稳定但响应更迟缓。

基于 Q(t)Q(t)
Q(t) 定义三级链路状态,如表4.2所示。当前阈值为理论设计值,将在4G实验完成后依据实测数据进行标定。

表4.2 链路质量三级状态定义
状态Q(t)Q(t)
Q(t) 范围
典型网络特征对应调度策略GoodQ(t)≥θGQ(t) \geq \theta_{\text{G}}
Q(t)≥θG​低延迟,低丢包计算密集任务卸载至地面站DegradedθP≤Q(t)<θG\theta_{\text{P}} \leq Q(t) < \theta_{\text{G}}
θP​≤Q(t)<θG​中等延迟,偶发丢包按优先级部分卸载PoorQ(t)<θPQ(t) < \theta_{\text{P}}
Q(t)<θP​高延迟,高丢包切换本地保守导航模式

[PLACEHOLDER-4.2.2-阈值: 参数 θG\theta_{\text{G}}
θG​、θP\theta_{\text{P}}
θP​、α\alpha
α、β\beta
β、RTTmax⁡\text{RTT}_{\max}
RTTmax的具体取值待4G实验后标定。标定方法:在不同网络条件下采集链路数据,以端到端延迟能否满足11 ms约束为判据,反推各状态的临界 Q(t)Q(t)
Q(t) 值。]


状态机引入迟滞(Hysteresis)机制防止状态边界处的频繁振荡:从高质量状态向低质量状态转移的阈值略高于反方向转移的阈值。具体地,当前状态为Good时,需要 Q(t)Q(t)
Q(t) 持续低于 θG-δ\theta_{\text{G}} - \delta
θG​-δ 才触发向Degraded的转移;而从Degraded恢复至Good,则需要 Q(t)Q(t)
Q(t) 持续高于 θG+δ\theta_{\text{G}} + \delta
θG​+δ,其中 δ\delta
δ 为迟滞带宽。

\subsection{任务调度问题形式化定义}

设系统中共有 NN
N 类可调度任务,任务集合 T={1,2,…,N}\mathcal{T} = \{1, 2, \ldots, N\}
T={1,2,…,N},任务 ii
i 在时刻 tt
t 的执行位置决策变量为:

xi(t)∈{local,offload},i∈Tx_i(t) \in \{\text{local},\ \text{offload}\}, \quad i \in \mathcal{T}xi​(t)∈{local,offload},i∈T
调度决策须满足以下约束:
约束一(时序确定性约束):神经刺激控制任务 istimi_{\text{stim}}
istim强制在边缘端本地执行,不参与动态调度:

xistim(t)=local,∀tx_{i_{\text{stim}}}(t) = \text{local}, \quad \forall txistim​​(t)=local,∀t
约束二(延迟约束):在任意调度决策下,端到端控制延迟须满足目标上界:

Ttotal(x(t))≤Tmax⁡=11msT_{\text{total}}\bigl(x(t)\bigr) \leq T_{\max} = 11\ \text{ms}Ttotal​(x(t))≤Tmax​=11ms
约束三(带宽约束):卸载任务的数据传输量不超过当前可用带宽:

∑i:xi(t)=offloadbi≤B(t)\sum_{i: x_i(t) = \text{offload}} b_i \leq B(t)i:xi​(t)=offload∑​bi​≤B(t)
约束四(链路状态约束):Poor状态下禁止卸载,避免在链路恶劣时产生卸载失败的雪崩效应:

xi(t)=local,∀i∈T∖{istim},若Q(t)<θPx_i(t) = \text{local}, \quad \forall i \in \mathcal{T} \setminus \{i_{\text{stim}}\},\ \text{若}\ Q(t) < \theta_{\text{P}}xi​(t)=local,∀i∈T∖{istim​},若Q(t)<θP​
优化目标是在满足上述约束的前提下,最小化最坏情况端到端延迟:
min⁡x(t)max⁡tTtotal(x(t))s.t.约束一至四\min_{x(t)} \max_{t}\ T_{\text{total}}\bigl(x(t)\bigr) \quad \text{s.t.\ 约束一至四}x(t)min​tmax​Ttotal​(x(t))s.t.约束一至四
选择minimax目标而非期望延迟最小化,是因为神经刺激控制对延迟的最坏情况更为敏感——一次超时比多次轻微延迟造成的危害更大。


\section{XXX任务动态调度算法设计}
WLTS由三个功能模块串行组成:链路质量感知模块负责实时评估4G链路健康度;静态-动态混合调度模块根据链路状态输出任务分配方案;容错降级模块处理链路严重退化时的安全保障。三个模块依次执行,形成完整的调度决策闭环。
\subsection{链路质量感知模块}
链路质量感知模块以固定采样周期运行,持续采集与地面站之间的RTT和丢包率,计算 Q(t)Q(t)
Q(t) 并维护状态机。算法4.1给出了完整的处理逻辑:

Algorithm 4.1: LinkQualityMonitor
Input:  RTT_samples[w]  —— 滑动窗口内最近 w 次RTT采样(ms)
        PLR_samples[w]  —— 滑动窗口内最近 w 次丢包率采样
        prev_state      —— 上一时刻的链路状态
Output: Q(t)            —— 当前链路质量指数
        state           —— 当前链路状态 ∈ {Good, Degraded, Poor}

1:  RTT_avg ← EWMA(RTT_samples, β)
2:  PLR_avg ← EWMA(PLR_samples, β)
3:  Q ← α × (1 - PLR_avg) + (1-α) × (1 - RTT_avg / RTT_max)
4:  Q ← CLAMP(Q, 0.0, 1.0)
5:  // 含迟滞的状态机转移
6:  IF prev_state == Good:
7:      state ← Degraded  IF Q < θ_G - δ  ELSE Good
8:  ELSE IF prev_state == Degraded:
9:      IF Q ≥ θ_G + δ:  state ← Good
10:     ELIF Q < θ_P - δ: state ← Poor
11:     ELSE:             state ← Degraded
12: ELSE:  // prev_state == Poor
13:     state ← Degraded  IF Q ≥ θ_P + δ  ELSE Poor
14: RETURN Q, state
滑动窗口大小 ww
w 的选取需要在响应速度和稳定性之间折中:ww
w 过小则对瞬时波动过于敏感,频繁触发状态切换;ww
w 过大则对链路退化的响应迟缓,可能在状态尚未更新时就已发生延迟违约。


[PLACEHOLDER-4.3.1: 参数 ww
w、β\beta
β 的具体取值待4G实验后通过灵敏度分析确定。分析方法:在不同参数组合下重放采集的4G链路数据,以状态切换次数和延迟违约率两个指标联合评估,选取帕累托最优解。]



\subsection{静态-动态混合任务调度策略}

调度策略的核心设计选择是将任务分为静态固定和动态调度两类,而非对所有任务采用统一的动态策略。这一选择的动机在于:动态卸载决策本身需要时间,且存在卸载-回收的切换开销;对于刺激控制这类硬实时任务,引入任何不确定性都是不可接受的。
Algorithm 4.2: WLTS_Scheduler
Input:  task_queue[]  —— 待调度任务列表(含任务类型和优先级)
        link_state    —— 当前链路状态 ∈ {Good, Degraded, Poor}
        B_available   —— 当前可用带宽(kbps)
Output: execution_plan —— 任务到执行节点的映射

1:  execution_plan ← {}
2:  FOR EACH task i IN task_queue(按优先级降序):
3:      // 静态规则:刺激控制强制本地执行
4:      IF task_type(i) == STIMULUS_CONTROL:
5:          execution_plan[i] ← LOCAL
6:          CONTINUE
7:      // 动态规则:依链路状态决策
8:      IF link_state == Good:
9:          IF bandwidth(i) ≤ B_available:
10:             execution_plan[i] ← OFFLOAD
11:             B_available ← B_available - bandwidth(i)
12:         ELSE:
13:             execution_plan[i] ← LOCAL
14:     ELSE IF link_state == Degraded:
15:         // 仅卸载高优先级计算任务(如YOLOv5推理)
16:         IF priority(i) == HIGH AND bandwidth(i) ≤ B_available:
17:             execution_plan[i] ← OFFLOAD
18:             B_available ← B_available - bandwidth(i)
19:         ELSE:
20:             execution_plan[i] ← LOCAL
21:     ELSE:  // Poor
22:         // 全部任务切换至本地执行
23:         execution_plan[i] ← LOCAL
24: RETURN execution_plan
三种链路状态下的调度行为存在本质差异:Good状态下系统充分利用地面站算力,边缘端仅承担刺激控制和视频采集;Degraded状态下采取保守策略,只将对延迟不敏感的高优先级计算任务(如YOLOv5推理,单帧处理时间约100 ms,本身就不在11 ms约束内)继续卸载,而将对延迟敏感的轻量任务切换为本地执行;Poor状态下则将全部可调度任务收回边缘端本地处理。
Degraded状态的处理体现了"部分降级"的设计思路:如果将Degraded直接等同于Poor处理(全部本地化),会导致地面站算力在中等网络波动时完全闲置,系统性能不必要地下降;而本文的方案允许在可行的前提下继续利用地面站资源,只将时延敏感任务切换本地,实现了性能与稳定性的均衡。
\subsection{容错降级机制}

链路质量感知模块提供的是统计意义上的状态判断,存在状态切换滞后的固有延迟。在状态尚未切换但链路已实际退化的时间窗口内,可能发生控制帧连续失败的情况。容错降级机制处理这种"感知-响应间隙",提供独立于状态机的安全保障:
Algorithm 4.3: FaultTolerantDegrader
Input:  link_state              —— 当前链路状态
        consecutive_fail_count  —— 控制帧连续失败次数
                                   (来自第3章mice-gRPC的ARQ失败通知)
Output: system_mode ∈ {Normal, Conservative, Emergency_Stop}

1:  IF link_state != Poor:
2:      consecutive_fail_count ← 0
3:      IF system_mode == Conservative AND link_state == Good:
4:          system_mode ← Normal
5:          notify_recovery()
6:      RETURN system_mode
7:  // Poor状态或连续失败处理
8:  IF consecutive_fail_count ≥ THRESHOLD_CONSERVATIVE:
9:      system_mode ← Conservative
10:     activate_conservative_navigation()
11:     notify_groundstation(DEGRADED)
12: IF consecutive_fail_count ≥ THRESHOLD_EMERGENCY:
13:     system_mode ← Emergency_Stop
14:     halt_navigation()
15:     broadcast_emergency_alert()
16: RETURN system_mode
Conservative模式下边缘端切换至本地保守导航策略,确保UAV和大鼠在无法接收地面站决策指令时仍能维持安全状态。

[PLACEHOLDER-4.3.3: Conservative模式下的本地保守导航算法待定义。待确认事项:(1)边缘端是否已有轻量级避障或直线巡航算法;(2)该模式下大鼠刺激指令的生成逻辑(停止刺激/维持最后指令/执行预设序列)。确认后补充此处算法描述,并在5.3.3节同步更新表5.4中的Conservative行为说明。]

THRESHOLD_CONSERVATIVE和THRESHOLD_EMERGENCY两个阈值的设定需要在安全性与可用性之间权衡:阈值过低会导致系统频繁进入降级模式影响导航效率,过高则在网络确实出现问题时响应过慢。

[PLACEHOLDER-4.3.3-阈值: 两个阈值的具体取值待实验确定。建议通过在tc netem模拟的不同丢包率条件下测量状态切换时间,以"从网络退化发生到系统进入Conservative模式的响应时间<500 ms"为约束,反推阈值上界。]

\section{算法复杂度与理论分析}

时间复杂度。 每个调度周期内,LinkQualityMonitor的计算复杂度为 O(w)O(w)
O(w)(EWMA计算需要遍历滑动窗口);WLTS\_Scheduler的复杂度为 O(∣T∣)O(|\mathcal{T}|)
O(∣T∣)(遍历任务队列)。在本文场景中,任务类型数量 ∣T∣≤8|\mathcal{T}| \leq 8
∣T∣≤8(刺激控制、视觉处理、路径规划、状态心跳等),远小于窗口大小 ww
w,因此每个调度周期的总时间复杂度为:

OWLTS=O(w+∣T∣)=O(w)O_{\text{WLTS}} = O(w + |\mathcal{T}|) = O(w)OWLTS​=O(w+∣T∣)=O(w)
WLTS算法本身的执行开销可忽略不计(预计在Jetson Orin NX上执行时间不超过0.5 ms),不会对系统整体延迟产生显著影响。
最坏情况延迟上界。
命题4.1:在Poor链路状态且所有任务执行WLTS静态分配(本地执行)的条件下,端到端控制延迟上界为:
TtotalPoor≤Tedgemax⁡+Tserialmax⁡≈2ms+3ms=5msT_{\text{total}}^{\text{Poor}} \leq T_{\text{edge}}^{\max} + T_{\text{serial}}^{\max} \approx 2\ \text{ms} + 3\ \text{ms} = 5\ \text{ms}TtotalPoor​≤Tedgemax​+Tserialmax​≈2ms+3ms=5ms
证明框架。 Poor状态下,Algorithm 4.2第22-23行强制所有任务在本地执行,Tnet=0T_{\text{net}} = 0
Tnet​=0。端到端延迟退化为纯本地处理时延之和,由表4.1中各固定分量的上界可得上述不等式。□\square
□
该命题说明,在最坏网络条件下,WLTS通过完全本地化将端到端延迟压缩至固定分量之和(约5 ms),反而低于Good状态下的典型值(约9.56 ms)。这是静态-动态混合策略的核心优势:网络越差,系统对网络的依赖越低,延迟反而更确定。
与EARN算法的理论对比。 EARN \cite{li2025earn}采用纯动态策略,通过模型预测控制决定任务执行位置,在机器人导航场景中验证了良好的性能。但其设计假设控制目标(机械执行器)对延迟抖动具有一定容忍度,且不区分不同优先级任务的调度策略。本文WLTS与EARN的主要理论差异如表4.3所示:
表4.3 WLTS与EARN算法理论对比
对比维度EARN \cite{li2025earn}WLTS(本文)调度策略纯动态(全任务参与卸载决策)静态-动态混合(刺激任务固定本地)延迟确定性动态切换引入不确定性刺激任务延迟完全确定弱网适应性无显式弱网感知机制三级状态机+迟滞保护生物控制适用性面向机械执行器设计针对神经刺激时序约束最坏情况保障依赖网络质量Poor状态下延迟≤5 ms(命题4.1)

\section{实验验证}
\subsection{实验设置}

WLTS算法的实验验证分两阶段进行:第一阶段使用Linux内核流量控制工具(tc netem)在Jetson Orin NX上模拟不同弱网条件,验证算法在受控环境下的行为;第二阶段在4G模块部署完成后使用真实4G链路验证算法的实际效果。
硬件平台配置:
组件配置角色地面站PCi7-12700H + RTX 5060 Ti,Windows 11高算力决策节点UAV边缘端Jetson Orin NX 16GB,Ubuntu 22.04,JetPack 6.x边缘调度节点网络仿真(第一阶段)tc netem(Linux内核流量控制)模拟4G链路特性网络通信(第二阶段)EC200A-CNHA 4G USB模块真实4G链路
tc netem通过在Jetson的WiFi网卡出口注入可控的延迟和丢包来模拟弱网场景:
bash# 模拟 Degraded 状态(中等弱网)
tc qdisc add dev wlan0 root netem loss 5% delay 30ms 10ms

# 模拟 Poor 状态(严重弱网)
tc qdisc replace dev wlan0 root netem loss 15% delay 70ms 20ms
对比基线:(1)纯动态卸载(EARN风格,所有非刺激任务均卸载,无降级机制);(2)静态本地执行(所有任务固定本地,无卸载);(3)原蓝牙集中式方案(文献数据)。

\subsection{延迟性能实验}

实验A(tc netem仿真)待采集数据:

四种网络条件(正常/丢包5%/丢包10%/丢包20%)×三种调度方案的P50/P95/P99延迟
建议采样次数:每组500次,重复3次取平均
建议用latency_probe.py工具采集,标签格式:wlts_good/degraded/poor_baseline/earn/static

实验B(4G真实链路)待采集数据:

室内/室外/有遮挡三种场景下的链路质量指数 Q(t)Q(t)
Q(t) 时序曲线

WLTS状态机切换记录(触发时刻、切换方向、切换前后延迟变化)
与netem仿真结果的对比(验证netem参数标定的准确性)



图4.2:不同网络条件下端到端控制延迟CDF曲线(图注占位)
横轴:延迟(ms),纵轴:累积概率(%),图例:正常/丢包5%/丢包10%/丢包20% 各四条曲线,红色虚线标注11ms设计目标
⚠️ [PLACEHOLDER-Fig4.2: 待实验数据采集后用matplotlib生成,建议使用seaborn风格]

\subsection{与现有方案对比}
[PLACEHOLDER-4.5.3: 本节待实验数据填入后完成。]
预期分析维度:

延迟确定性:WLTS在Poor状态下的P99是否显著低于EARN的P99
正常状态性能:Good状态下WLTS相对静态本地执行的延迟提升幅度
状态切换响应时间:从网络退化发生到系统调整完毕的时延
带宽利用率:不同链路状态下的地面站算力利用情况


表4.5 综合性能对比(待填入)
评价维度原蓝牙方案静态本地执行EARN风格动态WLTS(本文)正常网络P99延迟155–495 ms \cite{zhang2019bci}[PH][PH][PH]弱网(丢包10%)P99不可用[PH][PH][PH]延迟确定性(Poor状态)—高低高(命题4.1)地面站算力利用0%(本地集中)0%高状态自适应容错降级能力无—无有


\section{本章小结}
本章提出的WLTS算法从一个具体的工程矛盾出发:4G链路的动态性与神经电刺激时序的确定性需求之间存在根本性冲突,而现有面向机械机器人的动态卸载方案均无法处理这一冲突。WLTS的核心贡献在于明确区分了不可调度任务(神经刺激控制)和可调度任务(视觉处理、路径规划),前者通过静态分配获得时序确定性保障,后者通过链路感知动态调度最大化利用地面站算力。
三个功能模块各有侧重:链路质量感知模块将原始的RTT和丢包率数据融合为单一质量指数,并通过迟滞保护防止状态抖动;调度模块将抽象的质量等级转化为具体的任务分配决策;降级模块处理感知-响应间隙期间的安全保障,通过与第3章mice-gRPC协议的ARQ失败通知联动,在协议层和调度层形成双重容错。
目前算法设计已完成,实验验证待4G模块部署后展开。特别地,Poor状态下的本地保守导航策略尚未最终确定(见4.3.3节占位),这是第3/4/5章需要联合推进的工作,将在实验阶段与郑老师讨论后补充。
从系统架构的视角看,WLTS需要第3章mice-gRPC协议提供控制帧的可靠传输基础——ARQ重传机制的失败通知是FaultTolerantDegrader的触发信号之一;同时,WLTS的状态机输出也指导着mice-gRPC的优先级调度策略(Poor状态下暂停视频帧传输,让出全部带宽给控制帧)。两个创新点在运行时相互配合,构成本文应对4G弱网场景的完整技术方案.


\chapter{第4章 面向远距离导航的 mice-gRPC 通信协议}

\section{引言}
第3章提出的WLTS算法解决了地面站与UAV边缘端之间任务调度的宏观决策问题——在不同链路状态下,哪些任务应该卸载到地面站、哪些应该留在边缘端本地执行。但WLTS的有效运行依赖一个前提:当它决定将控制帧通过4G链路下发时,这条帧必须能够可靠、低延迟地到达对端。这一前提在WiFi局域网环境下几乎自然成立(实测P50=4.56 ms,P99=16.02 ms),但在4G远距离场景下却不再可靠。
问题出在标准gRPC协议的两个设计假设上。其一,gRPC基于HTTP/2构建,协议头开销在高带宽网络中可以忽略,但在4G窄带宽链路下,控制帧和视频帧共享有限带宽,协议开销会挤占有效传输空间。其二,gRPC依赖TCP的端到端可靠传输,TCP在检测到丢包时会触发拥塞控制,将发送窗口收缩至极小,在远距离无线链路上这种行为会导致吞吐量的断崖式下降。对于神经刺激控制这类对延迟有严格约束的场景,TCP拥塞控制引发的延迟抖动是不可接受的。
本章提出mice-gRPC(Minimal IoT Control Extended gRPC)协议,通过四项机制针对性地解决上述问题:控制帧轻量化压缩协议开销;优先级双队列保证控制帧先于视频帧发送;应用层ARQ在不触发TCP拥塞控制的前提下快速恢复丢失的控制帧;时分复用策略协调控制帧与视频帧在单通路上的共存。
本章与第3章的关系如下:WLTS调度算法的容错降级模块(Algorithm 4.3)依赖mice-gRPC的ARQ失败通知作为降级触发信号;反过来,WLTS的链路状态输出也指导mice-gRPC的调度策略(Poor状态下暂停视频帧发送,让出全部带宽给控制帧)。两个创新点在运行时相互配合,协议层的设计为调度层的决策提供了量化依据。


\section{问题分析与协议需求}
\subsection{动物机器人通信链路特性分析}
本文系统的通信链路分为两段:Jetson与背包端之间的蓝牙短链路(距离<1 m,仅用于边缘端本地通信,不在本章研究范围内),以及地面站与Jetson之间的4G长链路(距离可达数十米至百余米,是两个创新点共同作用的链路)。
针对4G长链路,本文已完成WiFi近距基线测量作为参照点:在地面站与Jetson距离约2 m的WiFi局域网条件下,以20 Hz频率连续发送500次gRPC HealthCheck RPC请求,测得RTT分布为P50=4.56 ms、P95=12.53 ms、P99=16.02 ms,0丢包。这一基线代表了理想网络条件下的系统性能下限。
WiFi基线数据揭示了一个重要信息:在链路质量充足的条件下,gRPC协议本身的开销(序列化、HTTP/2帧封装)对延迟的影响极小,P50仅4.56 ms中绝大部分是网络传播延迟。这意味着,4G场景下的主要延迟来源不是协议开销本身,而是4G链路固有的传播延迟(典型RTT 30–80 ms)以及信号波动引起的排队延迟。mice-gRPC的设计目标因此聚焦于:在4G链路质量给定的情况下,尽量减少协议层引入的额外延迟,并在帧丢失时快速恢复而不触发TCP拥塞控制。

[PLACEHOLDER-3.2.1: 待EC200A-CNHA 4G模块部署后,补充以下数据:(1)不同距离(5/20/50 m)和遮挡条件下4G链路的RTT分布;(2)4G链路的帧错误率(FER)随距离的变化曲线;(3)对比WiFi基线与4G基线,量化4G引入的额外延迟和不确定性。建议采样方法:与Day1基线实验相同,500次采样,标签格式"4g_Xm_indoor/outdoor/obstructed"。]

\subsection{标准gRPC在此场景的局限}

标准gRPC基于HTTP/2协议,每次RPC调用至少产生两种HTTP/2帧:HEADERS帧携带HTTP头部字段(方法名、内容类型等),DATA帧携带Protobuf序列化的消息体。HTTP/2 DATA帧头固定占用9字节(4字节帧长度 + 1字节帧类型 + 1字节标志位 + 3字节流标识符),首次RPC调用的HEADERS帧在头部压缩(HPACK)后通常还需要额外数十字节。
对于本文的控制指令场景,CommandRequest消息包含bag_id(字符串)、action(字符串)、CommandConfig(4个浮点数/整数字段)和timestamp(int64),Protobuf序列化后的消息体约占20–40字节。加上HTTP/2帧头和gRPC framing layer(5字节:1字节压缩标志 + 4字节消息长度),单次SendCommand RPC在网络上实际传输的字节数在60–80字节范围内。

[PLACEHOLDER-3.2.2: 待tcpdump抓包验证上述估算。建议在Jetson上执行:sudo tcpdump -i wlan0 -w grpc_capture.pcap "port 9001",同时在地面站发送100次SendCommand,用Wireshark或analyze_pcap.py分析实际帧大小分布。预期结果:DATA帧payload约20–40字节,含HTTP/2帧头后控制帧总大小约60–80字节。]

在200 kbps的4G窄带宽场景下(实际4G带宽会更高,但在信号边缘区域带宽可能下降至此量级),以20 Hz频率发送控制帧时,仅控制帧的带宽占用为:
Bctrl=75B×20Hz×8bit=12,000bps=12kbpsB_{\text{ctrl}} = 75\ \text{B} \times 20\ \text{Hz} \times 8\ \text{bit} = 12{,}000\ \text{bps} = 12\ \text{kbps}Bctrl​=75B×20Hz×8bit=12,000bps=12kbps
这一数字单独看并不严峻。真正的问题在于控制帧与视频帧共享同一4G链路时的竞争:场景B测试显示视频流带宽约13.9 Mbps(WiFi局域网条件下),即便经过压缩和码率自适应降低,在4G场景下视频帧的带宽需求也远超控制帧。当标准gRPC将两类帧一视同仁地放入发送队列时,视频帧由于体量更大,会频繁占据发送时隙,导致控制帧在队列中等待,引入不可预测的排队延迟。

\subsection{mice-gRPC的设计目标与约束}
基于上述分析,本文将mice-gRPC协议的设计目标形式化为以下约束集合:
帧大小约束:控制帧有效载荷须显著小于标准gRPC实现,以减少带宽占用和序列化耗时:

∣framectrl∣≤20bytes\left|\text{frame}_{\text{ctrl}}\right| \leq 20\ \text{bytes}∣framectrl​∣≤20bytes
延迟约束:控制帧的端到端传输延迟须满足WLTS调度算法设定的目标上界:

Tctrl≤Tmax⁡=11msT_{\text{ctrl}} \leq T_{\max} = 11\ \text{ms}Tctrl​≤Tmax​=11ms
可靠性约束:控制帧的帧投递率(Frame Delivery Rate,FDR)须超过安全阈值:

FDR=NdeliveredNsent≥99.9%\text{FDR} = \frac{N_{\text{delivered}}}{N_{\text{sent}}} \geq 99.9\%FDR=Nsent​Ndelivered​​≥99.9%
优先级约束:在任何带宽压力下,控制帧须优先于视频帧传输,不允许视频帧阻塞控制帧:

Tqueue,ctrl<Tqueue,stream,∀拥塞场景T_{\text{queue,ctrl}} < T_{\text{queue,stream}},\ \forall \text{拥塞场景}Tqueue,ctrl​<Tqueue,stream​,∀拥塞场景
兼容性约束:mice-gRPC须在标准gRPC服务接口层面保持向上兼容,地面站侧调用方代码无需修改,实现对Superowl现有导航算法的透明集成。
上述约束集合共同界定了协议设计的可行域。其中兼容性约束尤为重要——如果改造需要修改现有导航算法的调用方式,工程迁移成本将大幅增加,且容易引入新的集成错误。

\section{mice-gRPC 协议设计}

mice-gRPC的四项设计机制各自针对3.2节识别的一类问题,相互独立又协同工作:轻量化帧格式解决协议开销问题,优先级队列解决帧竞争问题,ARQ解决帧丢失问题,时分复用解决单通路共存问题。

\subsection{轻量化控制帧格式}

标准gRPC控制帧的主要开销来自两部分:HTTP/2协议头(固定9字节)和Protobuf消息体中冗余的字段元数据。mice-gRPC绕开HTTP/2帧结构,直接定义应用层的轻量化帧格式:
// mice-gRPC 轻量化控制帧(目标总大小 ≤ 20 字节)
message MiceControlFrame {
  uint32 frame_type = 1;  // 帧类型:0=刺激控制, 1=心跳, 2=ACK, 3=NACK
                          // Varint编码,占1字节
  uint32 seq_num    = 2;  // 序列号:0–255循环,用于ARQ去重
                          // Varint编码,占1字节
  uint32 stimulus   = 3;  // 刺激参数:高4位=方向(0–7), 低12位=强度(0–4095)
                          // 将原CommandConfig 4个字段压缩为位域编码,占2字节
  uint32 crc16      = 4;  // CRC16-CCITT校验值,覆盖前三字段
                          // 占2字节
}
// 总大小:约6–8字节(Protobuf Varint编码后)
// 含自定义帧头(4字节)合计约10–12字节,满足≤20字节约束
将原CommandConfig的4个字段(period、v_range、pulse_num、pulse_interval)压缩为单一uint32位域,是帧大小优化的关键步骤。方向字段(前进/左转/右转/停止)仅需3位;强度参数可以预先离散化为若干档位,用12位整数索引表示。这种压缩的代价是损失了参数的连续精度,实际应用中需要与神经刺激系统的参数分辨率需求匹配。

[PLACEHOLDER-3.3.1: 刺激参数的位域编码方案需要与背包端MCU的参数接收逻辑对齐确认。具体问题:(1)background端是否支持12位强度精度,还是实际只有若干离散档位;(2)方向编码是否与原串口协议的Channel字段完全对应。确认后更新此处的位域定义,并同步修改frame.py中的encode_stimulus()函数。]

CRC16采用CRC16-CCITT标准多项式(G(x)=x16+x12+x5+1G(x) = x^{16} + x^{12} + x^5 + 1
G(x)=x16+x12+x5+1),相比更简单的奇偶校验,CRC16能可靠检测所有单比特错误和大多数突发错误,同时计算开销仅需数十微秒(在Jetson上),不引入可测量的处理延迟。

\subsection{坏帧校验机制}

标准gRPC的HTTP/2多路复用机制对所有流(Stream)实行FIFO调度,不区分控制帧和视频帧的优先级。当视频帧连续进入发送队列时,即使在HTTP/2层面分配了更高的流权重,视频帧的体积优势仍然会导致控制帧被推迟发送。
mice-gRPC在发送端引入双队列机制,从根本上分离两类帧的发送路径:
Algorithm 3.1: PriorityQueueScheduler
Input:  control_frame  —— 控制帧(来自ControlService)
        stream_frame   —— 视频帧(来自StreamService)
        B_total        —— 可用总带宽(kbps)
Output: 有序发送序列

1:  ControlQueue ← HighPriorityQueue(maxsize=16)
2:  StreamQueue  ← LowPriorityQueue(maxsize=64)
3:  LOOP:
4:    // 新帧入队
5:    IF control_frame arrives: ControlQueue.enqueue(control_frame)
6:    IF stream_frame  arrives: StreamQueue.enqueue(stream_frame)
7:    // 优先调度:控制帧队列非空时优先发送
8:    WHILE NOT ControlQueue.empty():
9:        frame ← ControlQueue.dequeue()
10:       transmit(frame)
11:       B_remain ← B_total - bandwidth_of(frame)
12:   // 剩余带宽分配给视频帧
13:   WHILE NOT StreamQueue.empty() AND B_remain > 0:
14:       frame ← StreamQueue.peek()
15:       IF bandwidth_of(frame) ≤ B_remain:
16:           transmit(StreamQueue.dequeue())
17:           B_remain ← B_remain - bandwidth_of(frame)
18:       ELSE: BREAK
队列满时的丢弃策略非对称:控制队列满时丢弃最旧的帧(最新的控制指令优先),视频队列满时同样丢弃最旧帧(允许视频帧率下降)。这种设计确保了控制帧的低排队延迟——不论视频帧以什么速率进入,控制帧始终在当前发送周期的最开始被处理。
调度器的实际效果可以用一个简单的对比说明:在20 Hz控制频率和15 fps视频帧率的并发场景下,如果不使用优先级调度,控制帧平均需要等待约1/15≈671/15 \approx 67
1/15≈67 ms才能轮到发送机会(假设视频帧正在传输时);使用优先级调度后,控制帧的最大排队延迟仅为一帧控制帧的传输时间(约0.5 ms)。

\subsection{优先级重传机制}

gRPC依赖TCP提供可靠传输。当4G链路出现帧丢失时,TCP的丢包重传会在约一个RTT(30–80 ms)后重传,但同时触发慢启动:发送窗口收缩、吞吐量骤降、恢复过程持续数秒。对于需要20 Hz持续发送的控制帧,TCP的丢包恢复行为会造成后续帧的持续堆积延迟。
mice-gRPC在应用层实现ARQ,专门针对控制帧(视频帧不启用ARQ,实时性优于完整性):
Algorithm 3.2: ARQ_Retransmitter(仅对控制帧启用)
Input:  frame      —— MiceControlFrame
        timeout    —— 等待ACK超时时间(ms)
        max_retry  —— 最大重传次数
Output: status ∈ {ACK, FAIL}

1:  retry_count ← 0
2:  WHILE retry_count ≤ max_retry:
3:      transmit(frame)
4:      start_timer(timeout)
5:      response ← wait_for_ack(timeout)
6:      IF response == ACK: RETURN ACK
7:      ELSE:  // timeout或NACK
8:          retry_count ← retry_count + 1
9:          IF retry_count ≤ max_retry:
10:             sleep(2^(retry_count-1) × 1 ms)  // 指数退避
11: // 超过最大重传次数
12: log_frame_loss(frame.seq_num)
13: trigger_degradation_notify()  // 通知WLTS的FaultTolerantDegrader
14: RETURN FAIL
应用层ARQ相比TCP重传的优势在于:重传发生在应用层,不触发TCP的拥塞控制机制,后续帧的发送速率不受影响;同时ARQ的超时阈值可以根据系统的延迟预算精确设定,而TCP的重传超时(RTO)通常在数百毫秒量级,远超本文11 ms的约束。
超时阈值 TtimeoutT_{\text{timeout}}
Ttimeout的设定需要在控制延迟约束和重传机会之间权衡:

Ttimeout=Tmax⁡-Tedge-Tserial=11ms-2ms-3ms=6msT_{\text{timeout}} = T_{\max} - T_{\text{edge}} - T_{\text{serial}}
= 11\ \text{ms} - 2\ \text{ms} - 3\ \text{ms} = 6\ \text{ms}Ttimeout​=Tmax​-Tedge​-Tserial​=11ms-2ms-3ms=6ms

[PLACEHOLDER-3.3.3: Tedge=2T_{\text{edge}}=2
Tedge​=2 ms和Tserial=3T_{\text{serial}}=3
Tserial​=3 ms均为估算值(博士指出背包端延迟不在论文范围内,此处作为参考值使用)。实际 TtimeoutT_{\text{timeout}}
Ttimeout的最终取值建议在4G实测中通过扫描实验确定:在不同timeout值(3/5/7/10 ms)下测量帧成功率和端到端延迟的联合分布,选取满足FDR≥99.9%且P99≤11 ms的最大timeout值。]


最大重传次数 Nmax⁡N_{\max}
Nmax和指数退避策略的配合确保了在网络短暂抖动时快速恢复,同时避免持续重传加剧拥塞:第一次重传无退避,第二次退避1 ms,第三次退避2 ms。超过 Nmax⁡N_{\max}
Nmax次重传失败后,ARQ模块通知WLTS的FaultTolerantDegrader,后者据此判断是否需要触发系统降级。


[PLACEHOLDER-3.3.3-Nmax: 最大重传次数 Nmax⁡N_{\max}
Nmax待实验确定。理论上,在丢包率 pp
p 的链路上,ARQ重传3次后帧成功率为 1-p41 - p^4
1-p4;丢包率10%时成功率约99.99%,满足FDR≥99.9%约束。Nmax⁡=3N_{\max}=3
Nmax​=3 作为初始设计值,实验中验证。]


\subsection{单通路时分复用策略}

4G链路在物理层是单通路的:控制帧和视频帧最终共享同一个4G信道,无法真正并行传输。时分复用(TDM)策略将信道时间划分为固定周期 Tperiod=1/fcT_{\text{period}} = 1/f_c
Tperiod​=1/fc​(fcf_c
fc为控制帧发送频率),每个周期内优先分配控制帧时间片:

Tctrl_slot=∣framectrl∣×8BtotalT_{\text{ctrl\_slot}} = \frac{\left|\text{frame}_{\text{ctrl}}\right| \times 8}{B_{\text{total}}}Tctrl_slot​=Btotal​∣framectrl​∣×8​
Tstream_slot=Tperiod-Tctrl_slot-TguardT_{\text{stream\_slot}} = T_{\text{period}} - T_{\text{ctrl\_slot}} - T_{\text{guard}}Tstream_slot​=Tperiod​-Tctrl_slot​-Tguard​
其中 TguardT_{\text{guard}}
Tguard为保护间隔,防止控制帧与视频帧在物理层产生碰撞。在 fc=20f_c = 20
fc​=20 Hz(Tperiod=50T_{\text{period}} = 50
Tperiod​=50 ms)、控制帧大小12字节、Tguard=1T_{\text{guard}} = 1
Tguard​=1 ms的设计参数下:

Tctrl_slot=12×8BtotalmsT_{\text{ctrl\_slot}} = \frac{12 \times 8}{B_{\text{total}}}\ \text{ms}Tctrl_slot​=Btotal​12×8​ms
Tstream_slot=50-Tctrl_slot-1msT_{\text{stream\_slot}} = 50 - T_{\text{ctrl\_slot}} - 1\ \text{ms}Tstream_slot​=50-Tctrl_slot​-1ms

[PLACEHOLDER-3.3.4: BtotalB_{\text{total}}
Btotal​(4G实际可用带宽)待实测确定。在典型4G室内场景(信号强度>-90 dBm)中,下行带宽通常在5–50 Mbps范围,Tctrl_slotT_{\text{ctrl\_slot}}
Tctrl_slot的数值极小(<0.1 ms),Tstream_slotT_{\text{stream\_slot}}
Tstream_slot接近50 ms,视频帧可以充分利用剩余时隙。信号边缘区域带宽收窄时,WLTS的Poor状态会触发视频帧暂停,此时时分复用退化为控制帧独占信道。]



\section{协议实现}
\subsection{基于gRPC拦截器的扩展实现}

mice-gRPC通过gRPC的拦截器(Interceptor)机制实现,拦截器在gRPC调用链中插入自定义处理逻辑,对上层业务代码完全透明。这一实现方式直接满足了3.2.3节的兼容性约束:RemoteBagVJ.send_cmd()的调用方式不需要任何修改。
pythonclass MiceGrpcClientInterceptor(grpc.UnaryUnaryClientInterceptor):
    """
    mice-gRPC 客户端拦截器
    职责:轻量化帧构造 → 优先级队列入队 → ARQ等待ACK
    对调用方完全透明
    """
    def __init__(self, scheduler: PriorityQueueScheduler,
                 arq: ARQRetransmitter):
        self._scheduler   = scheduler
        self._arq         = arq
        self._seq_counter = itertools.cycle(range(256))  # 无锁原子递增

    def intercept_unary_unary(self, continuation,
                               client_call_details, request):
        # Step 1: 从CommandRequest提取刺激参数,构造轻量化帧
        mice_frame = MiceControlFrame(
            frame_type = FrameType.STIMULUS,
            seq_num    = next(self._seq_counter),
            stimulus   = encode_stimulus(request),
        )
        # Step 2: CRC16注入
        mice_frame.crc16 = crc16_compute(
            mice_frame.SerializeToString()[:-4]
        )
        # Step 3: 提交至高优先级控制队列,由ARQ负责发送和重传
        return self._scheduler.enqueue_control(
            continuation, client_call_details, mice_frame
        )
拦截器在intercept_unary_unary方法中同步完成帧构造和校验注入,然后通过enqueue_control异步提交到优先级队列,调用方收到grpc.Future对象后可以选择等待结果或继续执行。ARQ重传在后台线程中运行,不阻塞主线程的后续控制帧生成。

\subsection{CRC16校验模块}

CRC16-CCITT采用查表法实现,预先计算256项查找表,运行时每个字节仅需一次表查找和两次位操作,在Jetson Orin NX上的计算耗时约1–5 μs(对于6–8字节的控制帧),不引入可测量的延迟。
pythondef crc16_compute(data: bytes,
                  poly: int = 0x1021,
                  init: int = 0xFFFF) -> int:
    """
    CRC16-CCITT 查表法实现
    标准测试向量:crc16_compute(b"123456789") == 0x29B1
    """
    # 预计算查找表(模块加载时执行一次)
    table = _build_crc16_table(poly)
    crc = init
    for byte in data:
        crc = (crc << 8) ^ table[((crc >> 8) ^ byte) & 0xFF]
    return crc & 0xFFFF
选择CRC16而非更简单的校验和(checksum)的原因在于:在无线链路上,突发错误(burst error)比随机单比特错误更常见;CRC16能可靠检测长度不超过16位的所有突发错误,而简单校验和对突发错误的检测能力很弱。考虑到控制帧总大小仅6–8字节,CRC16的4字节校验开销(相对于帧大小)虽然比例较高,但绝对字节数(2字节)在设计约束范围内。

\subsection{与Superowl系统的透明集成}

mice-gRPC的集成点在RemoteBagVJ的初始化阶段:将标准grpc.insecure_channel替换为携带拦截器的channel,其余代码保持不变。
python# 改造前(标准gRPC)
channel = grpc.insecure_channel(f"{edge_host}:{control_port}")
stub    = ControlServiceStub(channel)

# 改造后(mice-gRPC,接口不变)
interceptor = MiceGrpcClientInterceptor(scheduler, arq)
channel     = grpc.intercept_channel(
    grpc.insecure_channel(f"{edge_host}:{control_port}"),
    interceptor
)
stub = ControlServiceStub(channel)  # stub使用方式完全不变
这一设计意味着,主窗口MainWin中对self.bag.send_cmd()的所有调用点无需修改,YOLOv5检测结果到控制指令的转换逻辑无需修改,录制和回放功能无需修改。mice-gRPC的优化效果对整个Superowl上层完全透明,这也是在已有工程系统上进行协议改造时降低集成风险的关键设计原则。

\section{实验验证}
\subsection{实验设置}

mice-gRPC协议的实验验证分两阶段进行,与第4章WLTS的实验安排一致。第一阶段使用tc netem在受控条件下验证各机制的单独效果;第二阶段在真实4G链路上验证端到端性能。
对比基线方案:(1)原蓝牙方案(文献数据,Zhang et al., 2019 \cite{zhang2019bci});(2)标准gRPC(无mice扩展,本文gRPC双机基线数据);(3)WiFi UDP(无连接无重传,作为延迟理论下界参照)。
表3.1 实验场景配置
测试场景环境距离网络条件场景1室内无遮挡5 / 10 / 20 m真实4G链路场景2室内有遮挡(墙壁)5 / 10 m真实4G链路场景3tc netem仿真—丢包率0/5/10/20%

[PLACEHOLDER-3.5.1: 实验环境待4G模块部署后确认。需要记录的额外信息:实验地点的4G运营商和信号强度(以dBm为单位),以便读者评估实验结果的外部有效性。]

\subsection{延迟与帧成功率测试}

[PLACEHOLDER-3.5.2: 本节全部数据待实验完成后填入。]
待采集数据:

不同距离(5/10/20 m)×三种方案(标准gRPC/mice-gRPC/WiFi UDP)的控制帧P50/P95/P99延迟
不同距离下mice-gRPC的帧成功率(FDR)
ARQ启用前后的帧成功率对比(在tc netem丢包率5/10/20%下)

预期分析:
若实测mice-gRPC在20 m距离下FDR≥99.9%而标准gRPC FDR<99%,
即可量化ARQ机制的可靠性提升;
若mice-gRPC的P99延迟低于标准gRPC且两者差值与
3.3.2节分析的排队延迟减少量吻合,即可验证优先级队列的有效性。


图3.5:不同距离下控制帧端到端延迟CDF曲线(图注占位)
横轴:延迟(ms),纵轴:累积概率(%),图例:标准gRPC vs mice-gRPC,按距离分组
⚠️ [PLACEHOLDER-Fig3.5: 待实验数据后用matplotlib生成]

\subsection{带宽占用对比}

[PLACEHOLDER-3.5.3: 本节数据待实验后填入。]
待采集数据:

控制帧+视频帧并发场景下的总带宽占用(tcpdump统计)
优先级队列启用前后,控制帧的平均等待时间
视频帧率在不同带宽压力下的变化(验证视频降级不影响控制帧)

预期分析:
轻量化帧格式将控制帧从约70字节压缩至约12字节(压缩约83%),
但仅凭帧大小减少并不能完全解决带宽竞争问题;
实验需要验证优先级队列的引入是否实质性降低了控制帧的排队延迟,
特别是在视频帧高速推流时控制帧的最大等待时间。

表3.4 带宽占用对比(待填入)
方案控制帧占用(kbps)视频帧占用(kbps)控制帧最大等待(ms)标准gRPC[PH][PH][PH]mice-gRPC[PH][PH][PH]

\subsection{ARQ重传效果分析}

[PLACEHOLDER-3.5.4: 本节数据待实验后填入。]
待采集数据:

不同tc netem丢包率(5/10/15/20%)下,ARQ启用前后的FDR
ARQ触发频率(每百次发送的平均重传次数)
ARQ引入的额外延迟开销(重传成功时的总延迟 vs 首次成功时的延迟)

理论预期:> ARQ重传 Nmax⁡=3N_{\max}=3
Nmax​=3 次,在独立丢包率 pp
p 下,
> 理论FDR为 1-pNmax⁡+11 - p^{N_{\max}+1}
1-pNmax​+1;
> 丢包率10%时理论FDR=99.99%,满足≥99.9%约束。
> 实验需验证实际FDR是否与理论值吻合,
> 以及突发丢包(burst loss)场景下ARQ的表现是否符合预期。



图3.6:不同丢包率下ARQ启用前后帧成功率对比(图注占位)
横轴:tc netem模拟丢包率(%),纵轴:应用层帧成功率(%),图例:无ARQ vs ARQ(Nmax=3)
⚠️ [PLACEHOLDER-Fig3.6: 待实验数据后生成]

\section{本章小结}

本章提出的mice-gRPC协议针对标准gRPC在4G窄带宽远距离场景下的两类具体缺陷展开设计:协议头开销与帧竞争问题,以及TCP拥塞控制不适合低延迟场景的问题。四项机制中,轻量化帧格式和优先级队列解决第一类问题,ARQ重传和时分复用解决第二类问题。
在工程实现层面,拦截器机制的选用确保了与现有Superowl系统的零改造集成——这一选择在论文写作之外也有实践意义:任何需要在运行中升级通信协议的系统,拦截器都是比直接替换底层接口更安全的路径。CRC16校验模块和seq_num去重机制在ARQ之外提供了额外的数据完整性保障,即使ARQ因某些原因未能及时检测到丢帧,CRC校验也会在接收端识别出损坏帧并触发NACK。
当前协议设计已完成,实现代码(frame.py、crc16.py、priority_queue.py、arq.py、interceptor.py)处于实现阶段,端到端集成测试待4G模块到货后展开。部分协议参数(超时阈值、最大重传次数、时分复用周期分配)在本章中给出了理论推导框架和[PLACEHOLDER]标注,将在实验阶段依据实测数据最终确定。
从与第4章的关系来看,mice-gRPC协议为WLTS算法提供了两个接口:其一,ARQ失败通知作为FaultTolerantDegrader的触发信号;其二,接受WLTS的状态输出,在Poor状态下暂停视频帧发送。这两个接口将协议层与调度层连接为一个整体,共同构成本文应对4G弱网场景的完整通信保障方案。第5章将在两个创新点均已就位的基础上,展示三层架构的整体部署效果和性能验证。

\chapter{第5章 面向动物机器人的三层异构协同控制架构设计}
本章在第3章mice-gRPC通信协议和第4章WLTS任务调度算法的基础上,系统阐述支撑两个创新点协同运行的三层异构协同控制架构的完整设计。架构设计的出发点是对现有单体架构局限性的精确诊断,以及对Rat-UAV协同导航任务的实际工程需求分析。

\section{系统架构需求分析}
\subsection{Rat-UAV单体架构的局限性分析}浙江大学课题组现有的大鼠机器人控制系统(Superowl)采用典型的PC集中式单体架构。在该架构下,大鼠背包端搭载摄像头和蓝牙通信模块,摄像头图像经蓝牙链路传输至地面站PC;地面站PC承担视频接收、目标检测、路径规划与控制指令生成的全部计算职责,并将最终的神经刺激控制指令经蓝牙链路回传至背包端执行。原系统的整体数据流如图5.1(左)所示。
这一架构在近距离受控实验室环境中运行稳定,但在Rat-UAV协同远距离导航场景下暴露出三类结构性局限:
局限一:通信距离约束。 蓝牙(BLE 4.x)的有效通信距离在开阔环境中约为10 m,在存在障碍物遮挡的实际场景中通常仅为3至5 m \cite{feng2007remote}。Rat-UAV协同导航的典型实验场地面积在5×55 \times 5
5×5 m至10×1010 \times 10
10×10 m量级 \cite{zheng2024ratuav},UAV引导大鼠在场地内自主巡航时,通信距离不足将直接导致指令中断。

局限二:通信延迟约束。 Zhang等人在脑-脑接口大鼠控制实验中实测蓝牙控制指令延迟为155至495 ms \cite{zhang2019bci}。在原单体架构下,该延迟叠加在"PC算法决策→蓝牙传输→背包执行"的全链路上,无法满足神经电刺激对时序确定性的严苛要求。
局限三:带宽单通路约束。 蓝牙4.x物理层净荷带宽约200 kbps,原架构中视频流与控制指令流共享这一单通路。当视频帧传输占用大量带宽时,控制指令将产生不可预测的排队延迟,两者相互竞争构成系统的核心性能瓶颈。
上述三类局限均源于蓝牙作为唯一通信信道的架构性约束,而非算法或硬件性能问题。因此,解决方案的核心在于架构层面的重构——将通信信道从蓝牙升级为支持远距离、高带宽的4G蜂窝网络,并在此基础上引入分布式计算节点承担实时控制职责。

\subsection{三层架构的功能需求}
基于上述局限性分析和Rat-UAV协同导航任务的实际需求,本文将系统重构为三层异构协同控制架构(如图5.1右所示),各层的功能需求如表5.1所示。
表5.1 三层架构功能需求分析
层级功能模块实时性要求计算复杂度地面站层YOLOv5目标检测≤100 ms/帧高(GPU加速)地面站层全局路径规划(BFS/GRNN/RL)≤500 ms/次高地面站层实验监控GUI异步更新低UAV边缘层Control Service(刺激指令转发)≤11 ms(端到端)低UAV边缘层Stream Service(视频采集推流)≥30 fps中UAV边缘层WLTS链路质量监测与调度≤100 ms/次极低背包端神经电刺激执行≤3 ms(串口响应)极低(MCU)背包端摄像头图像采集≥30 fps极低
非功能需求方面,系统须满足:端到端控制延迟小于11 ms(P99);视频帧率不低于30 fps;支持离线降级(4G链路断开时UAV边缘端可独立运行保守导航策略);支持x86(Windows/Linux地面站)与ARM64(Jetson Orin NX边缘端)的跨平台统一部署。
\subsection{异构平台约束分析}
三层架构的核心挑战之一在于三层节点之间存在显著的异构性,这一异构性是任务静态分配策略(而非完全动态分配)的根本工程依据。各层硬件资源对比如表5.2所示。
表5.2 三层异构平台资源对比
节点处理器内存操作系统网络接口功耗约束地面站PCi7-12700H + RTX 5060 Ti32 GB DDR5Windows 11 x86_64有线/WiFi,无约束无约束(市电)UAV边缘端Jetson Orin NX(8核ARM Cortex-A78AE)16 GB LPDDR5Ubuntu 22.04 ARM64,JetPack 6.xWiFi + 4G(EC200A-CNHA)≤25 W背包端MCU低功耗嵌入式MCU≤512 KB SRAMRTOS/裸机蓝牙(约200 kbps)≤100 mW
背包端MCU的极度资源受限(512 KB SRAM无法运行任何Linux发行版)决定了其只能作为被动执行层,不承担任何协议转换或计算卸载职能。Jetson Orin NX的100 TOPS算力(Super Mode下达157 TOPS)足以支撑Control Service和Stream Service的实时运行,同时具备在WLTS触发降级时本地运行轻量级路径规划的能力。地面站GPU的存在使YOLOv5推理和全局路径规划在该层执行具备显著的性能优势。


\section{三层协同架构总体设计}
\subsection{架构设计原则}
本文三层异构协同控制架构的设计遵循以下四条原则,各原则之间相互制衡,共同约束架构的设计空间:
原则一:实时性优先。 控制指令的端到端延迟约束(小于11 ms,P99)是最高优先级硬约束,所有架构设计决策在满足该约束的前提下进行优化。为此,神经刺激控制指令的生成与下发职责固定在UAV边缘端本地执行,不经由4G链路卸载,以消除网络抖动对刺激时序的影响。
原则二:资源感知分配。 任务分配须与执行节点的实际资源能力严格匹配。计算密集型任务(YOLOv5、路径规划)分配至具备GPU加速能力的地面站;实时性要求高但计算负荷低的任务(串口控制、视频采集)分配至UAV边缘端;硬件执行层(背包端)不承担任何计算职能。
原则三:向下兼容。 架构改造须对上层业务逻辑(Superowl实验管理系统)保持接口兼容。通过适配器模式(RemoteBagVJ替代MouseBagVJ、RemoteCameraVJ替代本地摄像头)实现微服务架构的透明接入,原有导航算法代码无需修改即可在新架构下运行。
原则四:容错降级。 系统须在4G链路质量退化时维持基本控制功能。降级策略须预先定义(对应第4章WLTS算法的降级机制),确保在Degraded和Poor链路状态下系统行为确定可控,而非出现未定义的失效模式。
\subsection{三层协同架构模型}
改造后的三层异构协同控制架构如图5.1(右)所示。架构由地面站智能决策层、UAV边缘计算层和背包硬件执行层三层构成,层间通过标准化通信接口连接。
第一层:地面站智能决策层。
地面站PC承担系统中所有计算密集型任务和人机交互界面。YOLOv5目标检测模型在RTX GPU上以不低于30帧每秒的速度处理来自Stream Service的视频流,输出大鼠位置和姿态估计结果;路径规划模块(支持BFS、GRNN、RL等多种算法)基于检测结果生成导航决策,通过gRPC的SendCommand RPC调用将最终的刺激指令类型(前进/左转/右转/停止)和刺激参数(脉冲宽度、电压幅度、脉冲数量、脉冲间隔)下发至边缘端Control Service。地面站还运行基于PySide6的实验监控GUI,提供实时视频显示、控制状态监控和实验数据记录功能。
第二层:UAV边缘计算层。
Jetson Orin NX(Ubuntu 22.04 ARM64)以Docker容器形式运行两个独立微服务:

Control Service(端口9001):接收来自地面站的SendCommand gRPC调用,将控制参数转换为14字节的串口协议帧(格式:Header(2) + Mode(1) + Channel(1) + Interval(2) + Num(2) + Width(2) + Voltage(2) + Trailer(2)),通过pyserial经蓝牙链路发送至背包端MCU。Control Service同时维护背包连接状态,提供ConnectBag、DisconnectBag、GetBagStatus、GetSensorData和HealthCheck共五个RPC接口。
Stream Service(端口9002):通过OpenCV的cv2.VideoCapture采集背包端摄像头的实时图像(640×480,30 fps),经JPEG编码后通过gRPC Server Streaming RPC(StreamVideo)持续向地面站推流。Stream Service还提供摄像头控制、视频录制和状态查询功能,与原Superowl系统的MouseBagVJ摄像头接口完全兼容。

此外,UAV边缘端运行第4章描述的WLTS链路质量监测模块,持续采集与地面站之间的RTT和丢包率,依据链路健康度动态调整任务卸载策略。
第三层:背包硬件执行层。
背包端的软件架构维持原有的成熟方案不变:嵌入式MCU通过蓝牙接收来自Jetson的串口帧,解析后驱动神经电刺激电路执行刺激动作;背包摄像头持续采集图像并通过蓝牙传输至Jetson的Stream Service。背包端与UAV边缘端之间的蓝牙通信距离在UAV飞行场景中不超过1 m(UAV悬停于大鼠正上方),不存在通信距离问题,本文不对该段链路进行改造。

\subsection{任务静态分配策略}

基于上述三层架构模型,系统采用静态任务分配策略确定各功能模块的固定执行节点,如表5.3所示。
表5.3 任务静态分配策略
任务名称执行节点分配依据实时性分类神经电刺激指令下发UAV边缘端(固定)延迟确定性约束,不经由4G网络硬实时(≤11 ms)视频流采集与推流UAV边缘端(固定)摄像头数据源在本地,避免原始流传输软实时(≥30 fps)链路质量监测(WLTS)UAV边缘端(固定)须本地感知4G网络状态软实时(≤100 ms/次)YOLOv5目标检测地面站(固定)需要GPU,边缘端无独立GPU用于推理非实时(≤100 ms/帧)全局路径规划地面站(固定)计算复杂度高,需大内存非实时(按需触发)实验监控GUI地面站(固定)人机交互界面必须在操作端非实时(异步更新)背包摄像头图像采集背包端(固定)数据源在背包端软实时(≥30 fps)神经刺激电路驱动背包MCU(固定)硬件直接控制,不可卸载硬实时
静态分配的必要性说明。 对于神经刺激控制指令下发任务,若采用动态分配策略(在地面站或边缘端之间运行时切换),则指令的执行节点和延迟特性在切换时刻将出现不确定性,违反刺激时序约束。因此,刺激控制任务的执行节点固定在UAV边缘端,不参与WLTS的动态卸载决策。WLTS的动态卸载对象仅限于视觉处理和路径规划等非实时计算密集型任务。

\section{微服务化部署设计}
\subsection{服务划分原则与边界定义}

Control Service与Stream Service独立部署为两个微服务,各自维护独立的生命周期和线程池,两者之间不存在直接调用关系。采用独立部署而非合并为单一服务的原因在于:Control Service为硬实时服务,任何Stream Service的阻塞(如摄像头帧编码耗时增加)不得影响控制指令的及时响应。独立部署后,操作系统的进程调度保障了两个服务之间的资源隔离。
Control Service接口定义(control.proto):
protobufservice ControlService {
  rpc ConnectBag    (ConnectBagRequest)    returns (ConnectBagResponse);
  rpc DisconnectBag (DisconnectBagRequest) returns (DisconnectBagResponse);
  rpc SendCommand   (CommandRequest)       returns (CommandResponse);
  rpc GetBagStatus  (StatusRequest)        returns (StatusResponse);
  rpc GetSensorData (SensorDataRequest)    returns (SensorDataResponse);
  rpc HealthCheck   (HealthCheckRequest)   returns (HealthCheckResponse);
}

// 控制指令消息定义
message CommandRequest {
  string bag_id   = 1;  // 背包ID
  string action   = 2;  // "front"/"left"/"right"/"stop"
  CommandConfig config = 3;  // 刺激参数
  int64 timestamp = 4;  // 请求时间戳(ms,用于延迟计算)
}

message CommandConfig {
  float period         = 1;  // 脉冲宽度(ms,1-10)
  float v_range        = 2;  // 电压幅度(V,1-10)
  int32 pulse_num      = 3;  // 脉冲数量(1-1000)
  int32 pulse_interval = 4;  // 脉冲间隔(ms,0-200)
}
Stream Service接口定义(stream.proto):
protobufservice StreamService {
  rpc StartCamera     (StartCameraRequest)     returns (StartCameraResponse);
  rpc StopCamera      (StopCameraRequest)      returns (StopCameraResponse);
  rpc StreamVideo     (StreamVideoRequest)     returns (stream VideoFrame);
  rpc GetStreamStatus (StreamStatusRequest)    returns (StreamStatusResponse);
  rpc StartRecording  (StartRecordingRequest)  returns (StartRecordingResponse);
  rpc StopRecording   (StopRecordingRequest)   returns (StopRecordingResponse);
  rpc HealthCheck     (HealthCheckRequest)     returns (HealthCheckResponse);
}
上述接口设计遵循以下原则:Control Service的SendCommand采用Unary RPC模式(单次请求-响应),延迟开销最小,适合硬实时控制场景;Stream Service的StreamVideo采用Server Streaming RPC模式,服务端持续推送帧数据,客户端无需反复发起请求,减少连接建立开销。timestamp字段在CommandRequest中用于在服务端记录指令处理延迟,为第4章WLTS算法的延迟分解分析提供数据支撑。
\subsection{跨平台部署方案}

系统须在x86(Windows/Linux地面站)和ARM64(Jetson Orin NX边缘端)两种异构平台上部署,通过Docker多架构镜像实现统一的部署流程。
多架构镜像构建:
bash# 构建支持 x86_64 和 arm64 的多架构镜像
docker buildx build \
  --platform linux/amd64,linux/arm64 \
  --tag superowl/control-service:latest \
  --push \
  -f Dockerfile.control .
Docker Compose服务编排(docker-compose.edge.yml):
yamlversion: "3.9"
services:
  control-service:
    image: superowl/control-service:latest
    ports:
      - "9001:9001"
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"  # 蓝牙串口直通
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c",
             "import grpc; grpc.channel_ready_future(
              grpc.insecure_channel('localhost:9001')).result(timeout=5)"]
      interval: 10s
      timeout: 5s
      retries: 3

  stream-service:
    image: superowl/stream-service:latest
    ports:
      - "9002:9002"
    devices:
      - "/dev/video0:/dev/video0"    # 摄像头直通
    restart: unless-stopped
    depends_on:
      control-service:
        condition: service_healthy
Docker容器化部署带来的核心优势在于:其一,依赖环境(grpcio、pyserial、opencv-python等)封装在镜像内,避免了ARM64平台上Python包编译兼容性问题;其二,restart: unless-stopped策略保证服务在Jetson意外重启后自动恢复,不需要人工干预;其三,healthcheck机制使Stream Service的启动依赖于Control Service就绪,保证服务启动顺序的确定性。
Windows地面站无需Docker部署,直接在venv_ground虚拟环境中运行gRPC客户端(RemoteBagVJ和RemoteCameraVJ),通过网络访问Jetson上的两个服务。
\subsection{服务容错与降级机制}
系统的容错机制分为两个层次:gRPC层的连接重试和应用层的功能降级。
gRPC层重连策略: RemoteBagVJ客户端实现了基于指数退避的自动重连逻辑,在检测到StatusCode.UNAVAILABLE时以1秒、2秒、4秒的间隔最多重试3次。重连期间,已下发的控制指令缓存在客户端队列中,待连接恢复后按序重发。
应用层降级机制: 基于第4章WLTS算法的链路状态输出,系统定义了四个运行状态,对应的服务行为如表5.4所示。
表5.4 系统运行状态与服务行为
系统状态触发条件Control Service行为Stream Service行为NormalQ(t)≥0.8Q(t) \geq 0.8
Q(t)≥0.8,所有服务健康
正常转发地面站决策指令正常推流,30 fpsDegraded0.5≤Q(t)<0.80.5 \leq Q(t) < 0.8
0.5≤Q(t)<0.8切换为部分本地决策,降低卸载量降低帧率至15 fps,节省带宽EmergencyQ(t)<0.5Q(t) < 0.5
Q(t)<0.5 持续超过阈值
完全本地保守导航模式暂停推流,让出全部带宽给控制帧Recovery链路质量回升至 Q(t)≥0.55Q(t) \geq 0.55
Q(t)≥0.55逐步恢复卸载策略逐步恢复帧率
\section{系统集成与实验结果分析}
本节从数据流角度验证三层架构的端到端运行效果,并通过实测数据与原单体架构进行性能对比。博士特别指出:从用户角度理解系统,关键在于"控制指令如何从PC一路跑到大鼠背包"以及"视频如何从背包一路回传到PC"这两条数据流——本节以此为主线组织分析。
\subsection{典型数据流端到端分析}
数据流一:控制指令流(地面站 → 背包端)
控制指令的完整路径如下:
① 地面站导航算法(YOLOv5检测 + 路径规划)
   生成控制决策:action="front",pulse_num=15,period=2.0ms

② RemoteBagVJ.send_cmd("front", cfg)
   构造 CommandRequest,注入纳秒级时间戳

③ gRPC Unary RPC → 4G网络 → Jetson Control Service
   [本文实测 WiFi近距P50=4.56ms,4G条件下预计30-80ms]

④ Control Service servicer.SendCommand()
   解析CommandRequest,组装14字节串口帧:
   [0xFF, 0xFF, mode, channel, interval(2B), num(2B), width(2B), voltage(2B), 0xFE, 0xFE]
   调用 MouseBagVJ.send_ctrl_opt(),通过pyserial写入蓝牙串口
   [串口传输延迟约2-3ms,9600波特率下14字节≈14.6ms理论下界]

⑤ 背包端MCU接收串口帧
   解析后驱动神经电刺激电路执行脉冲刺激
SERVICE_INTERACTION.md文档记录的延迟分解为:gRPC网络传输约8 ms(局域网实测),指令组装约2 ms,串口传输约3 ms,合计约13 ms,满足原50 ms设计目标。经过本次改造,以end-to-end P50=4.56 ms的近距WiFi基线数据验证,网络传输分量已达到优化目标,其余分量(指令组装、串口传输)为固定量,系统整体延迟在正常网络条件下满足11 ms约束。
数据流二:视频流(背包端 → 地面站)
视频数据的完整路径如下:
① 背包端摄像头采集
   640×480 @ 30fps,BGR原始格式

② 蓝牙传输至Jetson(约200kbps,短距离<1m)

③ Stream Service CameraStream.start()
   cv2.VideoCapture(camera_index, cv2.CAP_DSHOW)
   采集帧到frame_queue(maxsize=30的缓冲队列)

④ StreamVideo Server Streaming RPC
   JPEG编码(encode_time约5-10ms)
   封装为VideoFrame protobuf消息(含frame_number、timestamp、data字段)
   → gRPC Server Streaming → 4G网络 → 地面站

⑤ 地面站RemoteCameraVJ接收VideoFrame
   解码JPEG → numpy array → 传入YOLOv5推理
   [场景B测试实测:客户端解码帧率186.1 fps,视频流帧率10.3 fps,带宽13.9 Mbps]
场景B双机测试数据表明,在WiFi局域网条件下,视频流传输稳定无丢帧,带宽利用率不足30%(13.9 Mbps / 50 Mbps),具备充足的裕量支撑4G场景下的码率自适应降级。
\subsection{系统部署与运行效果}
实验环境配置。 本文完成了场景A(单机回环测试)和场景B(双机WiFi网络测试)两个阶段的部署验证,具体配置如表5.5所示。
表5.5 实验环境配置
配置项场景A场景B地面站Windows 11,RTX 5060 TiWindows 11,IP: 192.168.0.109边缘端localhost(同一台PC)Jetson Orin NX,IP: 10.193.120.87网络回环(localhost)浙大校园WiFi(10.193.0.0/16)Control Service:9001:9001Stream Service:9002:9002
服务启动验证。 Control Service在Jetson Orin NX上的启动日志如下,验证了ARM64平台上gRPC服务的正常运行:
Control Service 已启动
监听地址: [::]:9001
工作线程: 10
启动时间: 2026-03-28 10:xx:xx
✓ 服务已启动,等待客户端连接...(稳定运行时长:>9273秒)
Stream Service同样正常启动并提供视频采集服务,通过cv2.CAP_DSHOW后端解决了Windows平台摄像头枚举超时问题,启动时间约2.9秒。
场景B双机联调关键指标。 在地面站PC(192.168.0.109)与Jetson(10.193.120.87)通过WiFi联调的测试中,各项指标达到预期:

gRPC健康检查响应时间:<1秒(连接建立)
控制指令延迟(WiFi):P99 < 11 ms
视频流传输帧率:10.3 fps(与摄像头配置一致)
视频流传输带宽:13.9 Mbps
客户端解码速度:186.1 fps(解码能力充足,不构成瓶颈)
视频流丢帧数:0帧(1861帧完整传输)
\subsection{近距WiFi基线延迟实测}
实验设置。 在Jetson Orin NX与地面站PC同处一室(距离约2 m)的近距WiFi场景下,使用latency_probe.py工具向Control Service(:9001)连续发送500次HealthCheck gRPC请求,以20 Hz频率采样,统计端到端RTT分布,结果如表5.6所示。
表5.6 近距WiFi基线延迟测量结果(N=500)
统计指标数值(ms)说明P504.56典型场景下的稳定延迟P9512.5395%请求的延迟上界P9916.02偶发抖动的延迟上界均值5.54平均延迟水平最小2.81网络传输的理论下限最大35.05冷启动首次请求(第1次)
该基线数据揭示了以下关键信息:其一,近距WiFi场景下gRPC传输延迟P50=4.56 ms,系统具备满足11 ms端到端约束的基础能力;其二,P99=16.02 ms略超11 ms目标,其中冷启动效应(第1次请求14 ms)和偶发网络抖动是主要原因;其三,稳定后(第5次起)的延迟集中在3-8 ms区间,证明gRPC协议本身的开销极小,端到端延迟主要受制于网络传输质量。
将上述基线数据代入第4章延迟分解模型:
Ttotal=Tnet+Tedge+TserialT_{\text{total}} = T_{\text{net}} + T_{\text{edge}} + T_{\text{serial}}Ttotal​=Tnet​+Tedge​+Tserial​
=4.56ms+2ms+3ms=9.56ms<11ms✓= 4.56\text{ ms} + 2\text{ ms} + 3\text{ ms} = 9.56\text{ ms} < 11\text{ ms} \quad \checkmark=4.56ms+2ms+3ms=9.56ms<11ms✓
上述计算表明,在近距WiFi条件下系统满足11 ms约束,剩余约1.44 ms的裕量可用于应对偶发的额外系统开销。该结果同时说明:一旦进入弱网场景(4G链路延迟上升至30-80 ms),TnetT_{\text{net}}
Tnet将使端到端延迟超过11 ms目标,WLTS调度算法对此的介入必要性从实测数据层面得到了直接支撑。

\subsection{与原单体架构对比}

表5.7 改造前后系统性能对比
对比维度原单体架构(蓝牙)本文三层架构改进幅度控制延迟(P99)155–495 ms \cite{zhang2019bci}16.02 ms(WiFi近距)降低96.7%有效导航距离≤5 m(蓝牙范围)目标≥100 m(4G覆盖) [PLACEHOLDER: 待4G实测]提升>20倍控制+视频并行传输不支持(单通路竞争)支持(两条独立gRPC通道)新增能力弱网降级能力无(断连即全部失效)有(WLTS四态降级)新增能力跨平台部署仅Windows x86x86 + ARM64(Docker多架构)新增能力接口兼容性单体API100%兼容原MouseBagVJ接口零迁移成本

[PLACEHOLDER: 4G实测数据——待EC200A-CNHA模块到货后,在真实4G链路(移动/联通)上重复上述500次延迟测量实验,填入4G场景的P50/P95/P99数据;预期量级:P50约40-60ms,P99约100-150ms,WLTS降级策略在此场景下的效果将在第4章4.5节补充]

\section{本章小结}
本章系统阐述了面向动物机器人自主导航的三层异构协同控制架构的需求分析、总体设计、微服务化部署方案与实验验证。
架构设计以对原单体架构蓝牙通信局限性的精确诊断为起点,明确了三类结构性问题:通信距离约束(≤5 m)、端到端延迟约束(155-495 ms)和单通路带宽竞争。针对上述问题,本文提出将系统重构为"地面站智能决策层—UAV边缘计算层—背包硬件执行层"的三层架构,以4G蜂窝网络替代蓝牙作为主干通信链路,以gRPC微服务替代集中式串口通信作为服务接口规范。
在工程实现层面,两个微服务(Control Service :9001和Stream Service :9002)以Docker容器形式部署于Jetson Orin NX,通过多架构镜像构建实现跨平台一致性,通过适配器模式(RemoteBagVJ/RemoteCameraVJ)保持与原Superowl系统的接口兼容。场景A/B实测结果验证了架构的功能正确性:视频流传输稳定(10.3 fps,0丢帧),控制指令延迟近距WiFi基线P50=4.56 ms。
三层架构为第3章mice-gRPC协议提供了通信链路的部署上下文(4G链路上的控制帧与视频帧复用),为第4章WLTS算法提供了任务调度的执行环境(地面站—边缘端任务卸载决策空间)。三章在技术层面相互支撑、在实验层面共同验证,构成本文研究成果的完整工程体系。

\chapter{第6章 系统集成与实验结果分析}

\section{任务场景描述}
\subsection{动物机器人自动导航任务定义}


\subsection{关键技术指标体系}


\section{系统部署与运行效果展示}


\subsection{三层架构部署配置}



\subsection{系统运行效果}


\subsection{平台在任务中的关键作用分析}



\section{综合性能对比实验}
\subsection{与现有动物机器人系统对比}

```

\subsection{架构模式对比}


\subsection{通信协议对比}

\section{系统局限性讨论}

\section{本章小结}




\chapter{第7章 总结与展望}
\section{全文总结}
动物机器人作为一种新型的生物-机械融合系统,在灾害搜救、环境监测等领域具有独特的应用价值。然而,现有控制系统普遍面临控制延迟过高、集中式架构导致单点故障、缺乏分布式部署能力等问题,制约了动物机器人技术的进一步发展。针对这些问题,本文提出了一种面向动物机器人自动导航的地面站-边缘端协同控制架构,并完成了系统的设计、实现和验证工作。

本文的主要研究工作和贡献总结如下:

1)提出了面向动物机器人的地面站-边缘端协同控制架构

本文设计了一种三层分布式架构,将系统划分为地面站层、边缘端层和硬件层。边缘端部署Control Service和Stream Service两个微服务,分别负责控制指令处理和视频流传输;地面站部署导航算法和用户界面,通过gRPC协议与边缘端通信。这一架构实现了计算任务的合理分配:延迟敏感型任务刺激控制、传感采集)在边缘端就近处理,计算密集型任务视觉处理、路径规划)在地面站执行。相比传统的集中式架构,分布式设计提高了系统的可靠性和扩展性,支持地面站与边缘端的物理分离部署,为多动物并发控制奠定了基础。

2)实现了基于gRPC的低延迟控制协议

本文针对动物机器人控制场景的实时性需求,设计并实现了基于gRPC Unary RPC的控制协议。通过Protocol Buffers的高效序列化消息体积比JSON小3-10倍)、HTTP/2的持久连接和多路复用、以及串口通信的优化策略连接池管理、指令格式优化),系统实现了8-11ms的端到端控制延迟。与现有动物机器人系统BBI系统155ms、Rat-UAV系统500ms)相比,延迟降低了90\%以上;与REST API相比,延迟降低了约70\%。这一性能提升对于需要精确时序控制的神经科学实验具有重要价值。

3)实现了基于gRPC Server Streaming的视频流传输优化

本文采用gRPC Server Streaming机制实现视频流从边缘端到地面站的传输。系统支持JPEG和H.264两种编码方案,可根据网络条件和硬件能力自适应切换。通过帧缓冲管理双缓冲机制保证读写分离)和流控机制背压控制避免网络拥塞),在WiFi网络环境下实现了10fps稳定帧率、50-90ms端到端延迟、13.9Mbps带宽占用、零丢帧的传输性能。针对Windows环境下摄像头启动慢的问题,通过指定DirectShow后端将启动时间从超过10秒优化至2.9秒,性能提升71\%。

4)设计了基于适配器模式的渐进式系统改造方法

本文设计了RemoteBagVJ和RemoteCameraVJ两个适配器类,封装gRPC通信细节,对外提供与原有单体架构完全一致的接口。这一设计遵循"开闭原则",使得上层业务代码无需任何修改即可切换到分布式架构。实验验证表明,适配器与原接口的兼容性达到100\%,包括背包控制、摄像头管理、视频录制、传感器读取等全部功能。透明切换机制支持通过配置文件在本地模式和远程模式之间灵活切换,降低了系统改造的风险和成本。

5)实现了面向异构平台的统一部署方案

本文基于Docker容器技术,设计了支持x86\_64和ARM64双架构的统一部署方案。通过Docker Buildx多架构镜像构建,同一个镜像标签可以在Windows PC和NVIDIA Jetson等不同平台上自动选择对应版本运行。Docker Compose服务编排实现了多服务的一键部署,健康检查机制保证了服务的可用性监控和自动恢复。这一方案简化了在异构边缘设备上的部署流程,提高了系统的可移植性。

6)完成了系统的全面实验验证

本文在单机环境场景A)和双机网络环境场景B)下对系统进行了全面的测试和验证。功能测试验证了Control Service和Stream Service的独立功能以及跨服务的协同工作能力,离线视频导航集成测试验证了微服务架构对单体架构核心功能的完全兼容。性能测试表明系统达到了所有设计指标:控制延迟8-11ms目标<50ms)、视频帧率10fps目标≥10fps)、传输带宽13.9Mbps目标<50Mbps)。对比实验证明了gRPC相比REST在延迟和吞吐量方面的显著优势,以及本文系统相比现有动物机器人系统在延迟方面的数量级改进。
\section{工作展望}
基于本文工作的基础和存在的局限性,未来可以在以下方向开展深入研究:

1)真实环境部署与验证

下一步工作的首要任务是将系统部署到真实的动物机器人实验环境中进行验证。这包括:在NVIDIA Jetson Orin等边缘设备上测试系统的性能和功耗;连接真实的背包硬件验证串口通信的可靠性;在实际的大鼠导航实验中评估系统对实验效果的影响。真实环境的验证将为系统的实用化奠定基础。

2)H.264/NVENC硬件编码集成

充分利用Jetson平台的NVENC硬件编码能力,实现高效的H.264视频压缩。这将显著降低视频传输的带宽需求,使系统能够适应更广泛的网络环境。同时,可以研究自适应码率控制算法,根据网络状况动态调整视频质量,实现带宽与质量的最优权衡。

3)多动物协同控制扩展

扩展系统架构以支持多动物并发控制。这需要解决以下技术问题:设计基于服务发现机制如Consul、etcd)的动态服务注册;实现负载均衡策略以合理分配计算资源;设计多动物状态同步协议以支持协同导航实验。多动物控制能力将大幅提升系统的实验效率和应用价值。

4)边缘智能集成

探索在边缘端部署轻量级导航算法的可能性。随着边缘设备算力的提升,部分视觉处理和决策功能可以下沉至边缘端执行,进一步降低系统延迟。可以研究模型压缩、知识蒸馏等技术,将复杂的深度学习模型适配到资源受限的边缘设备上。边缘智能的集成将实现更低延迟、更高自主性的动物机器人控制。

5)云边端协同架构

将当前的地面站-边缘端两层架构扩展为云-边-端三层架构。云端负责大规模数据存储、模型训练和跨实验分析;边缘端负责实时控制和数据采集;端侧动物背包)负责刺激执行和传感感知。这种架构将支持更大规模的动物机器人实验,并为数据驱动的神经科学研究提供基础设施支撑。



% 本模板根据浙江大学研究生院编写的《浙江大学研究生学位论文编写规则》~\cite{zjugradthesisrules},
% 在原有的 zjuthesis 模板~\cite{zjuthesis}基础上开发而来。

% 本模板的本科生版本\cite{zjuthesisrules}得到了浙江大学本科生院老师的支持与审核,
% 已经在本科生院网上公示。
% 但当前的研究生版本并未经过研究生院老师的审核,
% 同学们使用时要注意对照模板与要求,
% 切不可盲目使用。

% 作者本人并未编写过浙江大学研究生毕业论文,
% 所以不清楚具体要求。
% 如果有热心同学愿意帮忙,
% 可以替我联系相关老师,我会配合审核并修改代码。

% 如果你在Overleaf上编译本模板,请注意如下事项:

% \begin{itemize}
%    \item 删除根目录的 ``.latexmkrc'' 文件,否则编译失败且不报任何错误
%    \item 字体有版权所以本模板不能附带字体,请务必手动上传字体文件,并在各个专业模板下手动指定字体。
%        具体方法参照 GitHub 主页的说明。
 %   \item 当前的Overleaf默认使用TexLive 2017进行编译,但一些伪粗体复制乱码的问题需要TexLive 2019版本来解决。
  %      所以各位同学可以在Overleaf上编写论文时务必切换到TexLive 2019或更新版本来编译,以免产生查重相关问题。
   %     具体说明参照 GitHub 主页。
%\end{itemize}


%\section{节标题}

% 我们可以用includegraphics来插入现有的jpg等格式的图片,
% 如\autoref{fig:zju-logo}所示。

% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=.3\linewidth]{figure/img/rviz.png}
%     \caption{\label{fig:zju-logo}Rviz}
% \end{figure}


% \subsection{小节标题}


% \par 如\autoref{tab:sample}所示,这是一张自动调节列宽的表格。

% \begin{table}[htbp]
%     \caption{\label{tab:sample}自动调节列宽的表格}
%     \begin{tabularx}{\linewidth}{c|X<{\centering}}
%         \hline
%         第一列 & 第二列 \\ \hline
%         xxx & xxx \\ \hline
%         xxx & xxx \\ \hline
%         xxx & xxx \\ \hline
%     \end{tabularx}
% \end{table}


% \par 如\autoref{equ:sample},这是一个公式

% \begin{equation}
%     \label{equ:sample}
%     A=\overbrace{(a+b+c)+\underbrace{i(d+e+f)}_{\text{虚数}}}^{\text{复数}}
% \end{equation}

% \chapter{另一章}


% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=.3\linewidth]{example-image-a}
%     \caption{\label{fig:fig-placeholder}图片占位符}
% \end{figure}

% \chapter{再一章}

% \par 如\autoref{alg:sample},这是一个算法

% \begin{algorithm}[H]
%     \begin{algorithmic} % enter the algorithmic environment
%         \REQUIRE $n \geq 0 \vee x \neq 0$
%         \ENSURE $y = x^n$
%         \STATE $y \Leftarrow 1$
%         \IF{$n < 0$}
%             \STATE $X \Leftarrow 1 / x$
%             \STATE $N \Leftarrow -n$
%         \ELSE
%             \STATE $X \Leftarrow x$
%             \STATE $N \Leftarrow n$
%         \ENDIF
%         \WHILE{$N \neq 0$}
%             \IF{$N$ is even}
%                 \STATE $X \Leftarrow X \times X$
%                 \STATE $N \Leftarrow N / 2$
%             \ELSE[$N$ is odd]
%                 \STATE $y \Leftarrow y \times X$
%                 \STATE $N \Leftarrow N - 1$
%             \ENDIF
%         \ENDWHILE
%     \end{algorithmic}
%     \caption{\label{alg:sample}算法样例}
% \end{algorithm}